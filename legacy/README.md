# Legacy

Compatibility and historical implementations preserved after normal production callers have migrated away.

Normal runtime code must not import from this directory. Keep material here only for explicit compatibility, forensic comparison, or controlled migration work. Deletion requires proof that no supported compatibility or migration path still needs it.

See `docs/CODE_RETIREMENT_AND_QUARANTINE.md`.
