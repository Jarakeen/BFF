# Dependencies actually needed to build and run the FoundryDock.exe build.
#
# The repo's top-level requirements.txt is missing PySide6 (the GUI
# framework the whole app runs on) and has a pile of unrelated
# packages (fastapi, uvicorn, pydantic, gspread, pyodide, jnius, js,
# xmlrpclib...) that come from engine/main.py, services/google_sheets
# _service.py, and other dev-only scripts nothing in the actual
# running app imports. Traced from what app.py's real import chain
# uses -- this is what's actually needed to build and run FoundryDock.exe.
#
# reportlab and keyring are both soft dependencies (the app already
# has fallback code paths if they're missing) but are included here
# so the packaged app has full functionality: PDF roster export
# (reportlab) and OS-vault-backed credential storage for the ESO Logs
# API key (keyring) instead of falling back to plaintext.

PySide6>=6.11
requests>=2.33
keyring>=25.7
reportlab>=4.4
pyinstaller>=6.22
