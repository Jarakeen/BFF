# Deprecated

Code that still works or may still have callers, but is being phased out and must not gain new production consumers.

Existing callers should be migrated to the canonical replacement before code is moved onward to `legacy/`, `migration/`, or deletion.

See `docs/CODE_RETIREMENT_AND_QUARANTINE.md`.
