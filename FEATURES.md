- **Performance Mode Build Matrix PDF export** — Builds PDF export now uses a low-ink, one-page-per-character Performance Mode matrix with a full Boss 1 baseline, mapped Boss 2/Boss 3/Trash/Flex Context Variant swaps, shared Always On race/Mundus/attributes/curse/Class Mastery/food/potions/CP, and a muted vector PM mark. The Builds dossier More actions menu adds **Export Build to PDF…** with strict Pydantic-validated export modes, variant-slot mapping, include toggles, canonical Context Variant resolution, and an all-variants-as-separate-pages option. A dedicated strict Pydantic export-source snapshot now validates identity, attributes, world state, gear, bars, CP, Class Masteries, consumables, and sparse Context Variants before any comparison or rendering; malformed source state fails closed and the exporter works from an isolated validated copy.\n- **Pre-EXE Pydantic persistence gate** — canonical Raid Plans, Build catalog identity graphs, Personnel, and Team schedules now have hardened validation/read-back boundaries; `tools/audit_user_data_persistence_gate.py` performs a read-only hash-verified audit of `foundrydock.db` before packaging and supports explicit duplicate-identity review without automatic fuzzy merges.\n- **Optimizer draft preview safety** — Team Optimization is explicitly labeled as draft/preview-only, keeps scenario persistence disabled, and can open any saved Raid Plan directly from the user-owned `foundrydock.db` selector without writing back to Raid Plans, Builds, Roles, Assignments, or Comp Maker.\n- **Raid Plan Crit/Pen cap indicators** — the existing Critical Damage & Penetration overview table keeps its original numeric columns and adds two compact per-player status columns: **CRIT STATUS** reports AT CAP vs BELOW, while **PEN STATUS** reports Physical and Spell as AT / BELOW / OVER against the remaining target resistance after raid Armor reduction. The underlying calculator and existing numeric display remain unchanged.
- **Coverage navigation checkpoint reconciliation** — when a saved Raid Plan changes outside the open planning page (for example after a revision restore), Coverage navigation now detects the newer persisted checkpoint and, when there are no unsaved visible edits, reapplies that exact saved plan before opening Coverage instead of forcing a manual reload. Unsaved edits still block navigation and remain protected.
- **Raid Plan Coverage persistence hardening** — loaded Raid Plans now carry the complete manual Coverage overlay through visible-editor reconstruction, and the repository preserves existing Coverage across every ordinary save, including optimistic `expected=` saves. Clearing Coverage is destructive and now requires the explicit `allow_coverage_clear=True` contract used only by the Coverage provider-removal path. This prevents Roles/Raid Plan/Assignments-style saves from erasing restored or manually assigned Coverage simply because their local UI model does not own that field.
- **Objective #32 runtime-scenario composition hardening** — candidate runtime-scenario assembly now validates supplemental event/history denominator flags as strict booleans, requires immutable tuple collections with canonical RuntimeEvent/history records, and passes downstream proof flags without truthiness coercion. This closes a remaining path where malformed truthy values such as `"false"` could be converted into authoritative proof before finite runtime-state composition.
- **Objective #32 weapon runtime sequence boundary hardening** — weapon-poison and weapon-enchantment sequence builders now require strict boolean event-denominator proof flags and canonical nested event/build/effect/policy records before finite history assembly. Poison cadence readiness must itself be a real boolean, and weapon-enchantment cooldown durations explicitly reject booleans instead of allowing Python's `float(True) == 1.0` coercion to become proof-state mechanics. Malformed inputs raise at the boundary; valid incomplete mechanics continue to fail closed through unresolved evidence.
- **Top Gear Support Gear idea board** — Top Gear now exposes **Support Gear** as its third visible tab. The workspace is split into **Healer Support Sets** and **Tank Support Sets**, each showing Set, Covers, and Notes. It seeds a practical current-meta starter list, persists user additions/edits/removals in the additive `support_gear_reference` table inside `user_data/foundrydock.db`, and provides role-local + Add Set / Edit / Remove actions. Set-name entry autocompletes from the canonical ESO gear catalog while still allowing free text, so the page stays useful as an evolving raid-lead idea board rather than pretending to be a fixed ranking.
- **Roles saved-plan picker and neutral Comp Maker start** — Roles exposes the canonical saved Raid Plan selector and guarded Load action directly; a fresh Comp Maker session starts with empty chairs and a Custom Goal, and requires an explicit plan name before its first save.
- **Canonical player-name presentation hardening** — planning pickers and Comp roster intake now resolve current human-readable player/character names from stable canonical IDs, suppress duplicate picker rows that share the same canonical player, preserve legacy rows without IDs as distinct, and avoid surfacing raw canonical player/build IDs in normal Roster presentation. Personnel management remains the place to inspect and explicitly merge real duplicate records.\n- **Roles saved-build picker canonical identity hardening** — Roles now filters saved Builds by stable `PlayerId` / `CharacterId` before legacy display text, so valid Builds remain available after Personnel merges, aliases, or character-label cleanup. Selecting a canonically owned Build also preserves the current Personnel display name instead of reintroducing a stale build alias.\n- **Brittle Uptime Review dashboard** — adds a dedicated Review surface for fresh ESO Logs Minor Brittle evidence across selected pulls, including raid-wide uptime, source-attributed provider contribution, pull comparison and provider charts, best/lowest/spread callouts, a compact evidence brief, and one-click clipboard export for sharing the observed pull evidence. Duplicate named Minor Brittle aura IDs are de-duplicated rather than summed, and no unsupported performance target is invented.
- **Weapon-enchantment one-hit runtime binding** — Objective #32 runtime scenario construction now consumes exact weapon-enchantment activation opportunities plus explicit per-opportunity cooldown-state evidence, resolves source-bar ownership and Dual Wield source selection, and emits at most one EffectVariant-bound runtime attempt for each isolated damage occurrence. Proven all-on-cooldown opportunities emit no proc attempt; missing cooldown truth remains a closure blocker.\n- **Explicit weapon-enchantment cooldown readiness** — Objective #32 can now evaluate an enchantment candidate as ready or blocked at an exact activation timestamp from caller-proven per-source cooldown state. The service owns timestamp arithmetic only and refuses missing, duplicate, or foreign state; it does not infer base cooldowns or shared-identity timer topology.\n- **Effect-bound runtime attempts** — the shared runtime proc stream can now bind one event attempt to one exact EffectVariant source using stable name/source/bar/source-slot provenance. Unbound attempts keep legacy shared-trigger behavior; bound attempts are ignored by every non-selected effect, giving weapon-enchantment source selection a generic one-hit exclusivity mechanism without inventing a second trigger taxonomy.\n- **Weapon-enchantment activation source resolution** — Objective #32 now composes exact enchant activation events, preserved source-bar ownership, and the finite one-hit source-selection frontier into one runtime/search contract. It can prove one exact source, retain multiple ready Dual Wield alternatives, or prove no proc when every owned source is on cooldown, while never calculating cooldown state from provisional cadence evidence.\n- **Weapon-enchantment source-bar ownership resolver** — Objective #32 can now narrow one exact enchant activation opportunity to canonical enchantment EffectVariants owned by the bar that fired the source action. Off-bar delayed damage therefore keeps its original weapon-bar ownership, while multiple same-bar Dual Wield candidates remain explicit for later source selection instead of being guessed away.\n- **Runtime source-bar provenance for weapon enchantments** — shared `RuntimeEvent` records can now preserve normalized `front`/`back` source-bar identity, and weapon-enchantment activation events carry the bar that fired the source action forward. This keeps Update 19 off-bar/source-weapon ownership usable downstream without pretending the currently active bar owns delayed damage.\n- **Canonical weapon-ability enchant occurrence classification** — Objective #32 now classifies each weapon-skill damage occurrence from reviewed per-coefficient component identity before creating enchantment activation opportunities. Direct hits and area DoT ticks remain eligible; single-target DoT ticks are excluded; incomplete coefficient/component shape fails closed instead of being guessed from timing or ability names. `ExtremeSustainedDPSWeaponEnchantmentActivationEventService.from_database()` wires the canonical classifier automatically.\n- **Authoritative off-bar weapon-enchantment source ownership** — cadence evidence now treats Update 19's source-weapon memory rule as authoritative: an activating weapon ability keeps the enchantment identity of the weapon that fired it even when its damage lands after a bar swap. This removes off-bar ownership from the Objective #32 enchantment blocker list without promoting still-open cooldown-sharing behavior.
- **Selected weapon-enchantment proc consequence binding** — Objective #32 can now map an exact source-bound glyph proc attempt back to the canonical equipped source and recover every combat-effect consequence produced by that one proc. Multi-output glyphs remain one proc with multiple consequences; unbound/foreign sources fail closed, and final damage/mitigation/crit/status/resource application remain separately owned.
- **Dedicated weapon-enchantment runtime-universe path** — Objective #32 can now replace the legacy triggerless “weapon enchantment runtime timing deferred” placeholder with canonical equipped-source and source-level runtime variants when both dedicated services are explicitly supplied. Other deferred mechanics and any enchant source/variant ambiguity continue to fail closed, while legacy callers keep the old blocker behavior.
- **Weapon-enchantment runtime-control/consequence separation** — Candidate runtime scenarios now keep source-selection glyph variants out of the combat-effect consequence set. The selector still drives exact enchant activation opportunities, cooldown/source-history branching, and bound proc attempts, but downstream stat/target effect projection receives only actual combat consequences rather than bookkeeping controls.
- **Canonical source-aware weapon-enchantment cooldown blockers** — The production cooldown-policy resolver can now reconstruct exact equipped enchant sources from the PlayerBuild, classify cadence family from canonical consequence rows, and report the precise remaining proof gap. Direct-damage glyphs reach the authoritative 4-second base-cooldown evidence but remain blocked on topology; Crusher-style buff/debuff glyphs remain blocked on base cooldown and topology.
- **Canonical weapon-enchantment cadence-family classification** — Objective #32 now classifies equipped glyph sources from canonical consequence semantics before applying cadence evidence: any canonical damage consequence makes the source direct-damage even when the proc also restores a resource, while reviewed non-damage buff/debuff consequences remain their own family. Restoration-only and unknown consequence shapes fail closed, and classification itself never assigns cooldown duration or timer topology.
- **Source-level weapon-enchantment runtime variants** — Objective #32 now projects each canonical equipped glyph into exactly one runtime source-selection EffectVariant regardless of how many combat consequences the proc produces. Source selection, cooldown sequencing, and attempt binding therefore operate on glyph identity while damage/restoration/buff/debuff consequences remain separately owned for post-selection projection.
- **Canonical weapon-enchantment runtime source projection** — Objective #32 can now resolve each equipped saved-build weapon enchant into one canonical semantic enchant identity plus exact front/back and main/off-hand provenance while preserving every trait-adjusted combat-effect consequence. Ambiguous labels, missing identity, empty consequences, or mismatched consequence provenance fail closed; cadence, cooldown topology, and runtime damage application remain separately owned.
- **Multi-consequence weapon-enchantment source identity** — Objective #32 now treats EffectVariants with the same canonical runtime binding provenance (effect identity, source, bar, and weapon slot) as one enchant source for source selection and cooldown sequencing. One selected proc attempt can therefore apply to every consequence variant of that enchant, while conflicting cooldown policies for the same source remain fail-closed.
- **Authoritative weapon-enchantment cooldown-policy resolver** — Extreme Engine now maps canonically classified weapon-enchantment EffectVariants into finite-sequence cooldown policies only when the full cadence denominator is authoritative. It refuses to infer direct-damage family from names or sources, keeps Crusher/buff-debuff cadence fail-closed, and uses `EffectVariant.name` as cooldown identity only after cooldown scope and same-identity sharing are proven.
- **Weapon-enchantment cadence blocker inventory** — cadence evidence now exposes exact machine-readable runtime blockers instead of only a `runtime_ready` boolean. Direct-damage and buff/debuff families identify which proof dimensions remain open, and `require_runtime_ready()` reports those exact gaps when exact simulation is attempted.
- **Weapon-enchantment one-hit exclusivity gate** — Objective #32 now fails closed when more than one weapon-enchantment runtime variant would share the same activation opportunity unless an explicit per-opportunity source-selection frontier exists. This preserves ZOS's rule that one isolated damage instance can proc at most one weapon enchantment while still allowing separate hits from multi-hit abilities to create separate opportunities.
- **Field-level weapon-enchantment cadence authority** — enchant runtime evidence now tracks authority separately for base cooldown, activation causes, off-bar persistence, cooldown scope, poison suppression, and same-identity cooldown sharing. ZOS-backed direct-damage 4-second cooldown and poison suppression can be used as proven facts without falsely promoting Crusher/buff-debuff cadence or source-sharing behavior.
- **Weapon-enchantment activation opportunity bridge** — Extreme Engine now has a canonical, skill-line-aware resolver for landed LA/HA/weapon-ability damage opportunities and a dedicated `weapon_enchantment_activation` runtime trigger family. The runtime scenario frontier can derive those opportunities from exact damage occurrences while keeping cooldown availability, source selection, poison replacement, off-bar persistence, and actual proc decisions separately fail-closed.
- **Authoritative weapon-enchantment activation topology** — the cadence evidence model now separates ZOS-proven activation causes from still-provisional cooldown topology. Landed Light Attack, Heavy Attack, and weapon-ability damage are primary-source activation events, including Dual Wield's off-cooldown source preference, while exact base cooldown/off-bar/poison/shared-cooldown questions remain fail-closed.
- **Objective #32 mechanics-gate parity** — the older generated Objective #32 wrapper now uses the same shared mechanics closure inventory as the canonical global wrapper. A finite search with complete mutation-axis proofs still cannot claim the theoretical maximum while canonical decision-critical mechanics coverage remains open.
- **Objective #32 end-to-end mechanics closure wiring** — the canonical global Extreme search now carries the shared mechanics closure inventory into both the theoretical-maximum proof gate and structured blocker report. If no explicit inventory is supplied, the shared canonical Objective #32 mechanics audit is used automatically, so complete finite search plus complete axes can no longer masquerade as theoretical closure while decision-critical mechanics coverage is still open.
- **Objective #32 typed runtime-scaling blockers** — runtime-effect scaling failures now report whether closure is blocked by missing candidate/runtime source evidence or by unreviewed math. Roar of Alkosh activation-time Weapon Damage is classified as source-data debt; unknown scaling formulas are math/review debt, and both flow into the shared Objective #32 closure inventory.
- **Objective #32 fail-closed runtime scaling gate** — candidate runtime EffectVariants with a non-empty scaling rule are no longer allowed to drift past the scaling layer unless that rule has an explicitly reviewed resolver. Master Architect remains resolved from canonical Ultimate spend; unreviewed rules such as Roar of Alkosh activation-time Weapon Damage scaling now block the candidate before relevance/runtime enumeration instead of risking a partially modeled proc.
- **Objective #32 inspectable runtime preflight** — strict theoretical-closure search no longer accepts a mere “runtime resolver exists” flag. Candidate-resolved runtime authority must expose proven supplemental event/history denominators plus canonical runtime EffectVariant discovery and scaling before traversal begins; otherwise the search fails closed at preflight.
- **Objective #32 closure-ready runtime scenario gate** — the canonical Extreme Engine composition now refuses global theoretical-closure traversal until the candidate-scoped supplemental runtime-event and supplemental runtime-history denominators are explicitly proven complete. A reviewed empty family is valid; missing proof is not. This catches incomplete proc/condition/group-history scenario ownership before expensive branch-and-bound work begins.
- **Objective #32 Master Architect duration scaling** — candidate runtime-state construction can now resolve Master Architect Major Slayer duration from canonical Ultimate spend (1 second per 10 Ultimate spent) before relevance/event enumeration; no-Ultimate branches treat the scaling as non-operative, while unresolved or incompatible Ultimate-cost evidence fails closed instead of silently using the nominal 1-second placeholder.
- **Objective #32 closure inventory and mechanics gate** — sustained-DPS closure now composes typed runtime source-data/math blockers with canonical mechanics coverage, feeds that inventory into the existing theoretical maximum gate, and surfaces the same debt through the canonical Objective #32 blocker report; partial relevant mechanics coverage keeps the theoretical claim open even when finite search and mutation-axis proofs are complete.
- **Objective #32 typed runtime blockers** — runtime relevance failures now distinguish missing source-data evidence from missing math/review routing while preserving the existing fail-closed unresolved contract; scenario evidence reports counts for each blocker class so closure work can target data acquisition versus engine math separately.
- **Weapon-enchantment runtime cadence blocker** — canonical Crusher magnitude/duration and trait-adjusted enchant effects are now separated from unresolved runtime cadence. Shared mechanics coverage marks weapon-enchantment trigger/base-cooldown/source-persistence evidence as decision-critical for Rotation Maker and Optimizer, preventing capability data from masquerading as uptime proof.
- **Saved-build Crusher capability bridge** — saved weapon enchantments now resolve canonical target-debuff EffectVariants through the existing enchantment repository/rule engine, preserving active bar, trait-adjusted magnitude, duration, and resistance reduction; Crusher runtime cadence remains an explicit fail-closed boundary until trigger/cooldown timing is authoritative.
- **Objective #32 generic numeric Damage Taken runtime routing** — fixed ENEMY-target damage_amplification metadata is projected into exact-time target CombatState and consumed by the canonical Damage Taken bucket alongside named Vulnerability; scaling amplification remains fail-closed, and named effects carrying duplicate numeric metadata are not double-counted.
- **Objective #32 explicit target mechanic metadata** — preserves resolved target damage-amplification, resistance-reduction, and penetration metadata on EffectVariant/gear-known-effect bridges; exact fixed ENEMY resistance reductions can now alter target mitigation at runtime, while scaling values remain fail-closed until their formula is proven.
- **Objective #32 runtime Brittle projection** — recognizes Major/Minor Brittle as ENEMY-target DPS-relevant runtime effects and adds their +20/+10 Critical Damage Taken to the attacker's raw Critical Damage before the shared 125% cap, then applies target Critical Resistance through the existing critical stage.
- **Objective #32 runtime Breach projection** — recognizes Major/Minor Breach as ENEMY-target DPS-relevant runtime effects, carries their exact windows in target CombatState, and applies 5948/2974 resistance reduction at each damage timestamp before mitigation, clamped at zero resistance.
- **Objective #32 runtime target-state projection** — carries the canonical runtime EffectVariant set with each generated runtime-state witness and projects active ENEMY-target Damage Taken windows, including reviewed Major/Minor Vulnerability, into the existing target_combat_state damage bucket at each exact damage timestamp. Target identity and expiration boundaries are preserved; effects never leak to a different enemy.
- **Objective #32 runtime-effect DPS relevance gate** — classifies candidate runtime procs before runtime-state enumeration. Canonically offensive/sustain-relevant effects remain searchable, proven defensive/healing-only effects can be removed from the DPS denominator, and unknown or target-side DPS effects that lack an exact Objective #32 projection remain fail-closed blockers rather than being assigned zero value.
- **Objective #32 candidate runtime effect universe** — derives runtime-triggered EffectVariants from the generated candidate’s canonical gear, active-bar skills, dynamic Champion Points, and potion availability seam; finalized potion-use effects remain plan-owned, unresolved identities fail closed, and active-bar set activation reuses the shared gear counter so off-hands and all two-slot weapon families agree with static mechanics.
- **Private-repository application updates** — packaged FoundryDock installs can use an app-specific update access file to check and download releases through the FoundryDock Update Gateway while the source repository remains private. GitHub credentials stay server-side; testers do not need GitHub accounts or tokens, and in-place updates continue to preserve local settings, builds, roster state, and `eso.db`.\n\n- **Urban Wilderness Raid Map formation presets** — the Raid Map toolbar includes reusable **House Stacks** and **Rainbow Stacks** layouts. Applying either preset arranges eight DD markers plus two healer markers and binds those ten players into a locked formation group by default, so dragging any grouped player moves the whole formation; the small lock button releases them for individual edits. Rainbow Stacks can rotate as a complete formation in 90-degree steps for room/boss orientation changes. Rename is always visible on the left side of **Layout & Output**, **Delete Selected** is the far-right destructive action, and Entrance/Exit/Banner reference controls live at the far right of the top Actors row. Only Timeline/Playback is collapsed behind **To Animate**, returning vertical space to the arena without hiding routine editing controls.\n

- **Live Raid operational lower workspace**: Next 60 Seconds, the Run Sheet, and a stacked Coverage / Recent Events column now expand into the available raid-running space instead of leaving a large unused lower canvas.

- **Finch Collaboration empty-state cleanup**: collaboration guidance and attention counts share one compact card, while an intentional empty state explains where shared Team, Raid Plan, Readiness, and Coverage snapshots will appear.

- **Consistent Collectibles progress cards**: every dashboard category now uses the same artwork → horizontal progress bar → count → status anatomy, eliminating mixed ring/shield/vial layouts across neighboring cards.

- **Raid Plan compact context bar**: Raid Plan and Assignments share one full-width Trial / Difficulty / Plan / Saved Plan control strip with grouped plan actions, keeping page titles uncluttered and page-specific actions such as Share Builds or Save Assignments in the same predictable place.
- **Portable Raid Plan backups**: Raid Plan can export the current plan to a small versioned `.raidplan.json` file containing only the complete Raid Plan payload. Restore previews the plan identity, requires explicit confirmation, creates a full `foundrydock.db` safety snapshot first, restores only that one Raid Plan through the canonical repository, verifies exact read-back, and leaves Personnel, Teams, Characters, Builds, other Raid Plans, and `data/eso.db` untouched.
- **Raid Review journal** — Live Raid notes are stored per Raid Plan attempt and indexed by date → trial on a dedicated Review page, with the full saved note shown in a large reading pane plus attempt number, pull start/end time, and duration.
- **Top Gear naming** — the former Capabilities surface is presented as Top Gear; Raid Plan Review now opens the run-note journal instead of Top Gear.
# BFF / FoundryDock Feature Index

- **Raid Plan seat save guard** — loading a saved Build into Roles cannot change its chair's player or class through a programmatic picker signal. Roles, Assignments, and Comp Maker refuse a save that assigns one named player to multiple occupied seats; direct navigation to Assignments opens the current saved Raid Plan when its editor has no plan loaded. Assignments exposes the portable Backup action so unsaved visible work can be exported even if its database save conflicts.

- **Comp Maker refresh and identity checks** — repeated Qt signals from one edit share one visible refresh; a programmatic row selection no longer repeats the Why panel pass, and unchanged recovery drafts do not rewrite the file every timer tick. Candidate suggestions track the selected Personnel identity and saved catalog changes. A skipped occupied Comp Build reports the exact player name or duplicate Personnel match that blocked linking.
- **Save status and recovery draft idle work** — hidden pages no longer query persistence to update their save badges; visible Raid Plan identity checks reuse one catalog read and the existing Personnel service. Unchanged Raid Plan and Raid Map drafts are not rewritten on every timer tick. The vAS+2 timer repaints only while its page is visible and catches up from elapsed time when reopened.
- **Build template destination picker** — archived players and archived Personnel links are hidden; duplicate catalog names prefer the one active Personnel identity when unambiguous. Unresolved duplicate names are withheld with a Players review note, and copying a Build asks for replacement confirmation once.

- **Raid Plan save conflict guard** — Raid Plan and Comp Maker compare saves against the version loaded into that editor, preventing an older screen from replacing a newer plan. Ordinary Raid Plan saves rely on transactional per-plan revisions without copying the whole user database. Reloading the same plan or starting a new one prompts for unsaved changes; recovery draft checks run only while the editor is visible.

- **Objective #32 effective potion cooldown evidence** — generated sustained-DPS search can resolve potion cadence from the finalized canonical build instead of relying only on a caller-supplied cooldown number. Canonical Objective #32 closure composition now requires that fail-closed cooldown authority rather than permitting a raw cooldown seam to stand in for proof. Passive cooldown evidence carries an explicit completeness contract, so empty, partial, or competing passive inventories cannot silently become a 45-second assumption. Jewelry cooldown items, canonical static cooldown EffectVariants, passive-grant inventory, and external scenario effects are composed through the shared potion-cooldown authorities; incomplete passive/scenario inventories and unresolved canonical build adaptation fail closed before potion timing can be treated as proven.
- **Objective #32 static-context preflight** — generated exact sustained-DPS leaves now refuse simulation when the shared Phase 5 static build context retains unresolved mechanics, preventing partially modeled passive/gear state from being scored as complete. Potionless generated candidates also bypass irrelevant potion-cooldown proof while retaining the canonical base cadence value for the no-op legality seam.

- **Comp Build round-trip identity** — Comp Maker can promote a real Personnel row into canonical player/character identity when no prior Build exists, save the resulting Comp Build under a stable BuildId, preserve that BuildId and Comp metadata through later Builds-page edits, and Readiness can open the exact selected build by stable ID.

- **Readiness canonical evidence** — Raid Readiness now resolves selected canonical/Comp Builds, distinguishes partial Comp plans as PLANNED instead of falsely missing, and reuses the same saved-build + planned-gear + planned-skill + assignment Coverage evidence as the Coverage page. Rotation and Sustain remain explicitly NEEDS REVIEW until their owning engines provide evidence.

- **Comp Maker Build Persistence** — saving a Phase 14 Comp Maker plan promotes every real occupied chair with a planned setup into the canonical build catalog, assigns a stable build ID back to the chair, and lets the Raid Plan persist that exact identity. Comp Builds use the normal build model with `BuildKind="comp"`, source plan/seat metadata, and partial planned gear/skill fields; repeat saves update the same Comp Build, recruits are skipped, and ordinary Saved Builds are never silently overwritten.

- **Phase 14 deterministic combat-simulation kernel** — consumes canonical `EffectiveBuildSnapshot` + Phase 13 `RotationPlan`, preserves exact action ordering and active-bar authority, fails closed on identity/bar violations, and emits explicit unresolved consequences until existing resource/healing/damage/proc engines are wired.
- **Phase 14 healer Magicka simulation bridge** — projects named healer skill costs and ordinary recovery through the existing Rotation/Phase 4 sustain authority, merges auditable resource transitions into the deterministic simulation stream, and keeps non-resource skill outcomes explicitly unresolved until their owning engines are connected.
- **Phase 14 healer output bridge** — reuses canonical healer component math to emit direct-heal simulation events and explicit periodic/delayed/channel healing seeds; exact periodic tick placement remains unresolved until reviewed runtime timing evidence is available.
- **Phase 14 reviewed healer periodic timing** — expands periodic-heal seeds into deterministic heal ticks only from canonical cadence/duration plus explicitly reviewed runtime timing/refresh evidence; supports reviewed fixture loading without promoting candidate evidence or guessing fixture paths.
- **Phase 14 reviewed healer skill effects** — projects reviewed bounded cast effects into deterministic apply/expire windows, reusing canonical skill-effect identity and runtime stacking; Combat Prayer Minor Resolve is the first control, while exact group recipients remain unresolved until target state is modeled.
- **Phase 14 exact-time simulation snapshots** — reads active bar, current resource amounts, canonical active effect windows, and unresolved boundaries from an already-computed deterministic simulation without recalculating ESO mechanics.
- **Phase 14 explicit recipient binding** — carries known self/ally/enemy combatants and exact event-scoped recipient bindings through simulation and snapshots; missing, unknown, or scope-conflicting recipients remain unresolved rather than being inferred from roster membership or geometry.
- **Phase 14 recipient-aware Health state** — applies explicitly bound direct/periodic heals to explicit current/max Health, recording attempted heal, applied heal, overheal, and exact-time Health snapshots without assuming missing target state.
- **Phase 14 incoming damage Health state** — accepts explicit post-mitigation incoming damage for known recipients, records applied damage/overkill, sequences it with healing, and projects resulting Health into exact-time snapshots without inventing mitigation or encounter damage values.
- **Phase 14 outgoing damage Health state** — accepts explicit already-resolved outgoing damage for known enemy combatants, carries it through the deterministic event stream into enemy Health/overkill/death transitions, and exposes the result through exact-time snapshots without duplicating canonical DD formulas or mitigation math.
- **Phase 14 canonical DD-to-simulation bridge** — reuses the existing Rotation per-action damage-evidence provider to turn resolved skill, Light Attack, Heavy Attack, and Ultimate consequences into explicit simulator outgoing-damage events for a known target. Provider UNKNOWN/unresolved evidence remains blocking, exact candidate identity can be preserved, and the simulator still owns no ESO damage formula.
- **Phase 14 saved-build DD combat simulation** — composes the canonical saved-build DD action provider at service level and feeds real skill/Light Attack/Heavy Attack/Ultimate damage evidence into the deterministic simulator for explicit enemy Health/death projection. Static DD relevance filtering is preserved, target resistance is explicit, execute/target-state evidence stays fail-closed, and UI code is not required by the simulation layer.
- **Extreme MOST Sustained DPS simulation bridge** — the Extreme Build Lab now exposes a simulation-backed sustained-DPS route for saved DD builds with saved canonical RotationPlans. The run requires explicit target Health and resistance, consumes Phase 14 Combat Simulation + Damage Summary, withholds a DPS record when damage evidence is incomplete, and reports the result only as a saved-build lower bound while global build/rotation search remains open.
- **Extreme sustained-DPS candidate comparison** — the Optimization layer can compare multiple explicitly supplied saved DD build/rotation witnesses through the same simulation-backed evaluator and target scenario. It ranks modeled witness values for inspection but only exposes a unique leader when every supplied candidate is modeled, mechanically complete, uses the same executed horizon, and the top value is not tied. This is candidate-set comparison, not proof of the global Extreme maximum.
- **Extreme sustained-DPS saved-state discovery** — scans the canonical saved build library for explicitly DD/DPS builds with stable canonical build identity and valid saved RotationPlan artifacts. Non-DD rows are retained as informational exclusions; DD/DPS rows with stale identity or missing/invalid rotations remain blocking evidence rather than disappearing from the denominator.
- **Extreme sustained-DPS saved-state search** — composes canonical discovery with candidate comparison to evaluate every eligible saved DD/DPS build/rotation witness under one explicit target Health/resistance scenario. It exposes a leader only when the saved denominator is mechanically complete and comparable, and explicitly labels the result as saved-library scope rather than a theoretical ESO-wide maximum.
- **Extreme sustained-DPS generated structural frontier** — begins Level 4 global generation by reusing the canonical Extreme global-search universe to enumerate deterministic race × legal class-route × 64-point attribute-allocation × active-bar coordinates. The frontier is pageable and countable without materializing the full Cartesian product, while gear, traits, glyphs, Mundus, consumables, skills, CP, passives, and runtime/rotation state remain explicit deferred axes rather than fabricated build details.
- **Extreme sustained-DPS proof-safe pruning** — adds the shared Level 4 pruning contract for generated search. The pruner consumes only externally proven optimistic DPS ceilings, discards a branch only when its ceiling is strictly below the current legal incumbent, keeps equal ceilings open to preserve tie/unique-leader semantics, and forces missing or unproven bounds open rather than guessing.
- **Extreme sustained-DPS dynamic axis inventory** — refines one generated structural candidate with canonical currently-supported Mundus choices, mechanically distinct mapped food/drink choices, modeled armor traits, modeled CP160 Truly Superb armor enchants, and bar-eligible active-skill identities filtered by the legal class route. Static refinement state is not mislabeled as a sustained-DPS ceiling; pruning remains fail-open until an optimistic action/rotation bound is proven.
- **Extreme sustained-DPS rotation upper-bound aggregation** — converts externally proven per-action optimistic total-damage ceilings into one whole-plan DPS ceiling over an explicit RotationPlan horizon. Every scheduled DD action must be covered exactly, and each accepted bound must include its direct, periodic, and triggered consequences inside the horizon; missing or partial evidence withholds the ceiling instead of permitting unsafe pruning.
- **Extreme sustained-DPS action upper-bound promotion** — aggregates exact canonical direct/periodic action occurrences for one concrete witness, but promotes that exact total into a pruning ceiling only when an explicit dominance proof covers every still-open mutation axis. Exact present-state damage is therefore kept distinct from optimistic future-build bounds.
- **Extreme sustained-DPS Mundus × provisioning dominance** — exhausts the canonical joint Mundus and mapped provisioning choice grid for one exact action witness while all other dimensions remain fixed. The largest resolved action total becomes an absolute proof-safe ceiling for those two axes only when every combination resolves; one unresolved combination withholds dominance and keeps the branch open.
- **Extreme sustained-DPS armor trait/enchant frontier** — preserves the complete modeled trait/enchant product for the armor slots actually equipped by a concrete witness, counts the full denominator, and exposes deterministic indexed/paged access without materializing the Cartesian product. No max-resource-specific trait/glyph reduction is reused for DPS.
- **Extreme sustained-DPS jewelry frontier** — lazily preserves the joint modeled jewelry-trait × canonical glyph-family denominator for equipped Necklace/Ring slots; no independent-stat DPS reduction is assumed.
- **Extreme sustained-DPS weapon frontier** — lazily preserves weapon-trait × canonical enchant-family identities for equipped front/back weapon slots while leaving enchant proc, cooldown, Infused, buff/debuff, and damage evaluation to runtime combat simulation.
- **Extreme sustained-DPS Champion Point frontier** — preserves every canonical slottable Champion Point star and the canonical four-slots-per-discipline legality denominator, with lazy mixed-radix access across discipline combinations rather than materializing the full loadout product. Dynamic star effects remain runtime/evaluator-owned.
- **Extreme sustained-DPS gear topology frontier** — reuses the canonical 12-unit active-snapshot set-count topology authority to expose pageable abstract gear branches before named-set expansion. It proves count partitions only, not physical equipment.
- **Extreme sustained-DPS gear topology realization** — expands one topology branch on demand into exact named-set slot witnesses with breakpoint, Mythic, armor/jewelry/weapon eligibility, and symmetry legality preserved. Truncated branches are explicitly exploratory; exhaustive empty branches are proven empty.
- **Extreme sustained-DPS dual-bar gear frontier** — composes all exhaustively realized topology branches into complete front/back equipment states only when shared body/jewelry assignments agree and canonical bar-access legality is satisfied; incomplete branch coverage keeps the denominator open.
- **Extreme sustained-DPS gear runtime semantic inventory** — classifies every active front/back set-bonus row through the reviewed static resolver and verified runtime-effect registry after canonical activation rules (including Torc suppression). Unsupported active bonuses remain explicit blockers; runtime identity alone never claims proc uptime or DPS contribution.
- **Generated sustained-DPS runtime evaluation** — evaluates one explicit hypothetical DD build + caller-owned progression + legal dual-bar gear state + RotationPlan + authoritative runtime history + explicit target through the existing Phase 14 Combat Simulation stack. Bar-legal named gear buffs and reviewed timed stat EffectVariants both affect exact runtime build contexts.
- **Sustained-DPS timed runtime stat projection** — carries already-active reviewed runtime EffectVariants into the canonical stat-input pipeline without owning trigger/cooldown timing. The first reviewed mapping projects temporary `weapon_spell_damage` windows into simultaneous Weapon Damage + Spell Damage contributions at the exact runtime instant; unknown identities still fail closed.
- **Extreme sustained-DPS potion-family frontier** — deduplicates canonical Alchemy formulas by exact mechanical trait family, retains a legal no-potion state, and materializes canonical `alchemy_family:` selections without assuming use timing or uptime.
- **Extreme sustained-DPS passive-rank frontier** — lazily enumerates rank 0 through canonical max rank for native candidate-class passives and passives on explicitly owned combat skill lines. It never grants shared line ownership implicitly and leaves racial progression on the race axis.
- **Extreme sustained-DPS skill-bar + morph frontier** — lazily counts and pages complete legal front/back bars without materializing the combinatorial product. Base skills and both morph alternatives remain distinct candidates, one skill family may appear at most once per bar, the same family may appear on both bars, empty slots remain legal, one-bar builds are supported, and shared combat lines require explicit ownership/equipment evidence.
- **Extreme sustained-DPS cross-axis context composition** — materializes one legal dual-bar gear state, derives front/back weapon lines from the actual equipped weapon pairs, derives armor lines from actual armor weights, preserves explicit shared-line ownership separately from class-route ownership, carries legal subclass lines, and applies Oakensoul one-bar access before skill-bar search. Unmapped scribed ability identity remains explicit unresolved evidence.
- **Extreme sustained-DPS generated candidate assembly** — merges selected CP, potion, passive-rank, and skill-bar coordinates onto one cross-axis-authoritative build without allowing any frontier's stale convenience snapshot to overwrite unrelated gear/class/identity state. The assembled coordinate is deterministic and one-bar/ownership contradictions fail closed.
- **Extreme sustained-DPS generated RotationPlan family frontier** — lazily enumerates every ordinary-skill ordering on each populated bar, both legal starting-bar routes for two-bar builds, and Light-Attack weave off/on over an explicit finite horizon. Plans are built with the canonical SemiStaticRotationPlanner and can pass through canonical duration refinement without choosing a DPS winner; Ultimate, potion, execute, Heavy Attack, encounter-demand, and broader policy families remain explicit later axes.
- **Extreme sustained-DPS Ultimate + potion policy frontier** — adds explicit no-Ultimate/front-Ultimate/back-Ultimate policy choices using canonical Ultimate affordability/spend projection, plus a finite anchored potion family consisting of no-use and before/after first-use ordering at seed-plan timestamps before the effective cooldown boundary, repeated at the caller-proven cooldown. Every result is checked by canonical scheduled-action resource legality. Continuous potion offsets and deliberate Ultimate delay after affordability remain open policy axes.
- **Extreme sustained-DPS execute policy frontier** — preserves the unchanged baseline plus a canonical execute-upgrade variant when reviewed execute thresholds are active and exact-slot canonical damage proves the execute is strictly better. Unresolved target Health or action damage remains blocking instead of becoming an assumed execute swap.
- **Extreme sustained-DPS Heavy Attack policy frontier** — enumerates every compatible subset of caller-proven safe 1.8-second fully charged Heavy Attack windows tied to exact ordinary skill slots. Same-timestamp Light Attacks are removed, overlapping/channel-colliding subsets are rejected, and canonical reservation provenance must promote every selected Heavy Attack into full-charge completion evidence before it can be scored by the dedicated Heavy Attack damage authority.
- **Sustained-DPS generated pipeline search runner** — binds one composed pipeline root, its exact-leaf evaluator, the shared comparison horizon, authoritative runtime snapshot, explicit target scenario, and optional root bound provider into the proof-safe lazy branch-and-bound coordinator. Missing bounds remain forced-open; all exact leaves are evaluated under the same scenario.
- **Sustained-DPS generated pipeline exact-leaf bridge** — extracts the final assembled build, explicit generated progression, physical dual-bar gear witness, and last downstream runtime-policy plan from a completed pipeline leaf, then delegates exact damage to the canonical generated-runtime Combat Simulation service and translates its result into branch-and-bound evidence. Missing or incomplete witness fields fail closed without running simulation.
- **Sustained-DPS generated-axis pipeline** — composes the gear, late build-refinement, rotation-plan, anchored-policy, execute, and reviewed Heavy Attack adapters into one immutable lazy tree. Stage transitions initialize each downstream state only after its upstream selections are complete, discard stale downstream selections after an upstream mutation, preserve axis-local optimistic-bound providers, and derive stable runtime candidate identities from the selected rotation coordinates.
- **Sustained-DPS generated runtime-policy wiring** — converts a selected anchored Ultimate/potion plan into the shared generated-rotation candidate form, then lazily enumerates canonical execute variants followed by compatible subsets of caller-reviewed 1.8-second Heavy Attack windows. Execute snapshot/target evidence and Heavy Attack windows remain explicit caller inputs; downstream compatibility is recomputed per upstream mutation and unresolved denominators fail closed.
- **Sustained-DPS generated rotation-axis wiring** — connects each assembled build to the finite seed/cadence RotationPlan family and then to the anchored Ultimate/potion policy family as ordered lazy axes. Explicit Ultimate-generation and Heroism evidence is forwarded unchanged; unresolved denominators fail closed, and continuous potion offsets, delayed Ultimates, execute policy, Heavy Attacks, and encounter obligations remain open proof scopes.
- **Sustained-DPS generated gear-axis wiring** — connects a denominator-proven dual-bar gear catalog to armor, jewelry, and weapon trait/enchant refinement as ordered lazy axes. Each mutation receives the build produced by the previous axis, preserving all selected gear state before the resulting cross-axis context proceeds to CP, potion, passive, and skill search.
- **Sustained-DPS late-axis wiring** — connects the canonical Champion Point, potion-family, passive-rank, and two-bar skill frontiers to the lazy indexed search tree in dependency order. Selections remain separate until canonical candidate assembly applies only each axis’s owned state; unresolved denominators fail closed before materialization.
- **Sustained-DPS generated-frontier wiring** — presents caller-supplied canonical indexed frontiers as one lazy search tree with stable coordinates, dynamic child counts, inherited/tightened proof-safe bound envelopes, and exact-leaf delegation. It never materializes the full Cartesian product and does not take ownership of frontier legality, ESO damage math, or Combat Simulation.
- **Generated sustained-DPS proof-safe branch-and-bound coordinator** — lazily expands generated branches, preserves missing/unproven-bound branches as forced-open, prunes only proven-safe ceilings strictly below the current incumbent, evaluates leaves through caller-supplied exact canonical simulation, enforces one shared comparison horizon, preserves tied maxima, and distinguishes “global maximum proven” from “unique leader proven.”
- **Generated sustained-DPS search evidence adapter** — bridges canonical whole-rotation upper bounds into pruning evidence and generated Combat Simulation results into exact leaf evidence without strengthening incomplete proof. Unknown bounds remain unknown and incomplete simulation never becomes a numeric leaf by convenience.
- **Sustained-DPS partial-branch upper-bound envelope** — combines multiple independently proven optimistic ceilings for the same generated branch by taking the tightest safe bound, never by adding overlapping contributions. Child branches inherit valid parent ceilings and may tighten them with more specific proof; missing or heuristic local evidence cannot weaken an already proven envelope.
- **Sustained-DPS structural action-count upper bound** — computes a pruning ceiling only when callers prove both the maximum number of damage-bearing scheduled actions over the exact horizon and an absolute optimistic total-damage ceiling for any one action that already includes all direct/periodic/triggered consequences. Missing proof keeps the branch open instead of inventing an ESO action-rate assumption.
- **Sustained-DPS closed-descendant absolute action ceiling** — when a partial branch has a finite proven-complete descendant denominator, complete canonical consequences for every damage-bearing action across every descendant can prove the branch's absolute per-action maximum. Direct/periodic/triggered coverage must be explicit, and the resulting ceiling is scoped only to that closed branch.
- **Sustained-DPS canonical axis dominance composition** — defines one generated-search mutation-axis vocabulary and composes independent proof coverage by union while keeping numeric optimism separate. Unknown axes are rejected, missing required axes remain visible, and only complete coverage can be promoted into an action-dominance proof.
- **Sustained-DPS Mundus × provisioning proof adapter** — converts the existing complete finite joint Mundus/food action search into canonical `{mundus, food}` dominance coverage plus its proven absolute per-action ceiling. The pair remains coupled; an incomplete grid promotes neither half.
- **Sustained-DPS gear + progression axis coverage adapters** — promote complete dual-bar named-gear closure into `gear_topology` + `named_gear_realization`, complete CP closure into `champion_points`, and complete passive-rank closure into `passive_ranks`. These are structural dominance facts only; traits, enchants, runtime mechanics, racial progression, and numeric action ceilings remain separate.
- **Generic sustained-DPS finite-axis action dominance** — exhausts any caller-supplied proven finite axis denominator for one exact scheduled damage action, requiring every choice to resolve the same action coordinate and complete canonical consequences. A complete search simultaneously proves canonical axis coverage and the maximum exact per-action damage for those axes, allowing CP/passive/gear slices to reuse one proof engine.
- **Sustained-DPS indexed finite-axis frontier adapters** — expose complete CP, passive-rank, and dual-bar named-gear denominators as lazy `choice_count + choice_at(index)` sources for the generic dominance engine. Large frontiers are not eagerly materialized; each canonical choice is built only when evaluated.
- **Canonical finite-axis exact-action evaluators** — map CP choices onto candidate builds, passive-rank choices onto explicit generated progression, and dual-bar gear choices onto canonical materialized equipment, then delegate one fixed scheduled action to the Combat Simulation DD provider for exact occurrence evidence. Runtime/periodic/weapon gaps remain unresolved rather than becoming zero damage.
- **Reusable sustained-DPS CP/passive/gear dominance searches** — provide one composition entry point per finite family, automatically combining the lazy frontier adapter, canonical exact-action evaluator, and generic finite-axis proof engine. Callers receive canonical axis coverage plus the numeric action ceiling without manually wiring the three layers.
- **Sustained-DPS combat-dynamic axis coverage** — promotes complete legal skill-bar closure into `skill_bars`; complete semi-static seed rotation closure into `rotation_order` + `light_attack_weave`; finite anchored Ultimate/potion policy closure into `ultimate_policy` + `potion_timing_policy`; and complete execute/Heavy Attack policy families into their canonical axes. Continuous potion offset, deliberate delayed-Ultimate timing, and Heavy Attack windows outside the caller-reviewed safe set remain explicit omitted scope.
- **Finite whole-plan sustained-DPS dominance** — exhausts a proven finite dynamic family using complete modeled DPS at one shared horizon. Unlike action-level dominance, plan shape and action identity may differ across choices; a complete family promotes both canonical axis coverage and the maximum exact sustained-DPS score as a pruning ceiling while preserving omitted timing/encounter scope separately.
- **Generated whole-plan choice evaluator** — holds build, explicit progression, dual-bar gear, authoritative runtime history, and target assumptions fixed while varying only a legal dynamic RotationPlan and initial bar. Each choice is scored through the canonical generated Combat Simulation sustained-DPS path and unresolved runtime mechanics stay unresolved.
- **Sustained-DPS dynamic whole-plan frontier adapters** — expose seed-rotation, anchored Ultimate/potion, execute, and reviewed Heavy Attack policy families as lazy indexed whole-plan choices. Source denominator proof and omitted scope are preserved, so continuous timing and unreviewed Heavy Attack windows cannot disappear merely because a finite subfamily is searchable.
- **Finite runtime-state sustained-DPS dominance** — accepts only caller-supplied runtime-state families whose local denominator is explicitly proven closed, varies `ExtremeRuntimeSnapshot` while holding build/progression/gear/plan/target/horizon fixed, and scores every choice through generated Combat Simulation. A complete local family promotes `runtime_state` plus its exact finite-family DPS ceiling; omitted proc/cooldown/condition/encounter scope remains explicit.
- **Finite-family branch-scope pruning adapter** — converts a local finite-family whole-plan ceiling into branch-and-bound evidence only when the branch denominator is explicitly proven identical to that family and every omitted-scope item is explicitly excluded from the branch. This prevents local rotation/runtime closure from being misused as a wider global ceiling.
- **Generated-node finite-family pruning bridge** — lets the real lazy generated-axis tree consume a local finite-family ceiling only after the proof is tied to the exact generated node key. Node-aware bound providers can tighten root or child envelopes without changing axis state semantics; a proof naming a sibling branch fails closed instead of being rebound for convenience.
- **Theoretical MOST Sustained DPS closure gate** — separates “the finite generated tree is fully solved” from “Objective #32 is theoretically solved.” The stronger claim requires a proven finite search, complete coverage of every canonical sustained-DPS mutation axis, and zero explicitly omitted theoretical scope. Omitted timing/window scope now travels inside canonical axis proofs and is consumed automatically, so a successful finite branch-and-bound run cannot hide known-open theory.
- **Generated local runtime-state terminal axis** — a caller-proven finite runtime family can now be appended after the normal generated gear/late/rotation/runtime-policy pipeline. Each runtime choice is lazily indexed and reaches exact Combat Simulation as the selected snapshot; unproven or unresolved families are rejected, and omitted global runtime scope remains attached to the runtime_state axis proof.
- **End-to-end Objective #32 proof result** — runs the generated pipeline search, composes caller-supplied canonical axis proofs, automatically adds proven local runtime-state coverage when that frontier is searched, and then applies the theoretical closure gate. Theoretical closure additionally requires an explicit scope proof that those axis proofs describe the same generated search denominator/root. The returned result exposes best modeled DPS, finite-denominator proof, and theoretical proof separately rather than letting one optimistic boolean do three jobs.
- **Structural sustained-DPS coverage adapter** — converts a proven generated structural frontier into canonical race, class-route, and attribute coverage while refusing to treat the inherited active-bar coordinate as a separate Objective #32 axis.
- **Validated sustained-DPS structural family frontier** — collapses each front/back structural duplicate into one race/class/attribute family only after verifying the pair is identical except for active bar. This keeps rotation starting-bar ownership in one place instead of searching the same character twice for decorative reasons.
- **Structural family materialization** — turns each validated structural family into ordinary `PlayerBuild` and `CharacterProgression` state using the existing class-route and progression authorities, with no new ESO stat formulas.
- **Global generated sustained-DPS tree** — prepends the validated structural family axis to the existing gear → progression → skills → rotation → runtime generated pipeline. Race, class route, and attributes now correspond to real branch-and-bound branches rather than external coverage paperwork.
- **Generated search-axis inventory** — every lazy generated axis can declare the canonical mutation dimensions it physically enumerates. The inventory reports searched, missing, untagged, and duplicate canonical axes without confusing “present in the tree” with “denominator proven.” Global search includes structural race/class/attribute axes in this inventory automatically.
- **Generated encounter-policy axis** — a caller-proven finite encounter-policy family can now sit after exact build assembly and before rotation-plan generation. Each choice carries canonical `RotationDemandWindow` values into the existing `RotationDurationRefinementService` with explicit ability priorities, so encounter scheduling reuses Rotation’s reviewed demand machinery instead of creating Extreme-specific timing rules.
- **Reviewed encounter-policy registry bridge** — turns one selected encounter’s existing reviewed Rotation encounter-demand registry entry plus canonical boss-guide timing into the finite Extreme encounter-policy frontier. Explicit reviewed “no demands” is preserved as a valid choice; missing review, blockers, unresolved clock facts, and threshold policies without a proven threshold-to-clock projection stay open instead of quietly becoming nothing.
- **Exact-witness Mundus + food generated axes** — a proven finite Mundus/food denominator can now be inserted after gear and before CP/potion/passives/skills. Each choice mutates the real evolving build that reaches Combat Simulation, and the adapter emits canonical `mundus + food` coverage. The older action-level Mundus × provisioning dominance remains useful for pruning but is not allowed to choose the final whole-rotation witness.
- **Global Objective #32 proof runner** — derives canonical mutation-axis coverage directly from the exact completed generated tree, so same-tree theoretical closure no longer requires a caller-supplied scope certificate. Optional supplemental proofs are accepted only when an explicit scope proof ties them to the same denominator; otherwise they are ignored and reported. The generated-axis inventory must still confirm every canonical mutation axis is physically present, and all axis-carried omitted scope must be empty, before the theoretical flag can close.
- **Objective #32 structured closure blockers** — every global MOST Sustained DPS result now includes stable machine-readable blocker codes for finite-search incompleteness, physically missing or duplicate canonical axes, coverage gaps, unresolved evidence, and explicit theoretical omissions. This turns a bare `theoretical_maximum_proven=False` into an actionable list of exactly what still prevents proof.
- **Objective #32 canonical production composition** — the generated sustained-DPS architecture now has one explicit production composition root that wires structural families → generated pipeline → exact leaf evaluation → global generated search → global Objective #32 closure. Composition fails closed unless the late finalized-potion axis is present and its additional resource-event denominator is explicitly certified complete; low-level frontier/mechanics services remain independently owned.
- **Objective #32 composition requires canonical static axes** — the production composition root now refuses to assemble without Mundus/Food and Encounter Policy adapters, in addition to the complete Heavy Attack and finalized Potion timing proof paths. The generic generated pipeline remains optional/flexible for legacy and focused tests; only the canonical Objective #32 composition is strict.
- **Objective #32 scenario closure preflight** — canonical production searches now reject theoretical-closure traversal before branch-and-bound unless a complete, omission-free runtime-state frontier is supplied and the encounter's Heavy Attack channel-block denominator is proven complete. Generic finite-denominator wrappers remain usable without this strict mode.
- **Controlled Objective #32 runtime-state witnesses** — callers can now supply only genuinely external runtime history such as proc/event attempts, condition windows, and external group-buff applications. RotationPlan-owned bar transitions are injected/verified canonically and finalized potion timing stays plan-owned, removing both from the caller's runtime-state mutation scope.
- **Proven-empty runtime history** — `ExtremeRuntimeSnapshot` now distinguishes an authoritative empty runtime history from missing runtime evidence through `runtime_history_complete`. This allows a scenario with genuinely no external runtime events to remain a valid exact Objective #32 witness instead of failing because its history tuple is empty.
- **External-history runtime-state frontier** — a proven finite external-history family can now be composed directly into the ordinary canonical `runtime_state` frontier. Any invalid history, competing bar/potion truth, or unproven external-history denominator fails the full runtime-state family closed.
- **Finite runtime proc-evidence equivalence frontier** — caller-proven runtime event skeletons now expand into every materially distinct deterministic proc realization without arbitrary RNG sampling. Chance rolls collapse at canonical EffectVariant proc-chance thresholds, while relevant named conditions expand as explicit condition-context subsets. A proven no-event scenario becomes one empty evidence choice.
- **Supplemental runtime-history assembly** — finite proc-attempt evidence can be crossed with caller-proven supplemental condition-window/external-group-buff histories before runtime witness composition. A proven no-supplemental scenario contributes one empty choice rather than erasing the denominator.
- **Scenario-facing runtime_state builder** — Objective #32 callers can now provide a finalized plan/build, proven event skeleton family, canonical runtime effects, and supplemental external-history proof to receive the ordinary canonical runtime_state frontier directly. Full ExtremeRuntimeSnapshot construction is no longer a caller responsibility.
- **Runtime trigger skeleton authority split** — the candidate-facing runtime_state builder now derives scheduled skill casts, Ultimate activations, Light Attacks, verified Heavy Attack completions, and exact `damage_dealt` occurrences where canonical evidence exists. Potion-use state remains finalized-plan-owned. Unsupported encounter triggers such as synergy activation, corpse consumption, off-balance-target damage, or heal events remain explicit supplemental scenario families with denominator proof.
- **Candidate-scoped runtime effect universe** — Objective #32 can now derive the relevant runtime-triggered EffectVariant set directly from canonical SavedBuild capability evidence instead of requiring callers to hand-supply effects. Potion-triggered variants stay plan-owned, triggerless static/passive effects stay with static/conditional mechanics, and deferred runtime-effect conversions fail closed.
- **Candidate-resolved runtime_state axis** — canonical Objective #32 no longer reuses one global runtime frontier across every generated build. Runtime state is resolved after Heavy Attack and finalized Potion timing for each final candidate, so gear/skills/rotation-specific proc universes produce their own finite runtime denominator. Legacy focused searches may still append one static runtime frontier globally.
- **Delayed Ultimate timing frontier** — Objective #32 now enumerates every legal delayed Ultimate cast/skip sequence across the exact same-bar skill slots of each seed rotation and integrates that finite family into the generated Ultimate/potion policy axis. Same-timestamp generation is never borrowed ahead of a cast. The delayed-Ultimate theoretical omission is therefore removed for the searched seed-plan denominator; continuous potion timing remains open.
- **Scheduler-derived Heavy Attack window discovery** — Objective #32 can now probe every scheduled ordinary skill slot through the canonical soft-action duration scheduler to find every mechanically legal 1.8s fully charged Heavy Attack start for that finalized execute plan. First casts, due refreshes, bar/hard boundaries, horizon limits, and same-bar displacement are inherited from the existing scheduler rather than duplicated. Encounter channel prohibitions remain explicit reviewed intervals with a separately proven complete denominator.
- **Complete Heavy Attack policy mode** — the generated runtime-policy adapter now has a proof mode that discovers Heavy Attack starts per execute candidate, materializes every compatible subset through the canonical scheduler, and exposes omission-free `heavy_attack_policy` coverage only when the encounter channel-block denominator is proven complete. Legacy reviewed-window mode remains available and still carries its old omitted scope.
- **Potion timing breakpoint frontier** — continuous potion first-use time can now be reduced, for one finalized descendant plan, to a finite exact set of activation/expiry boundaries plus one representative from each open interval between them. The proof is explicitly scoped to named-buff runtime state at supplied observation points; exact same-timestamp before/after ordering is preserved for later materialization, and instant potion restoration timing remains separate until its canonical sustain bridge is included.
- **Finalized potion observation denominator** — Objective #32 can now collect the exact finite DD observation coordinates from one finalized descendant: scheduled skill/Ultimate/Light Attack points, verified Heavy Attack completion times, and every concrete reviewed periodic tick. Missing HA completion evidence or unresolved periodic projection keeps the denominator open instead of falling back to seed-plan timestamps.
- **Finalized potion timing denominator** — the finalized observation set now composes with source-backed `PotionUseEvent` data and the character's explicit Medicinal Use rank through `PotionCadence`, producing the exact effective named-buff durations used by the finite breakpoint frontier. This proves the named-buff timing family for a finalized descendant while preserving instant-restoration timing as separate proof scope.
- **Potion resource-event observation frontier** — Objective #32 now over-partitions potion restore timing by every scheduled action timestamp, ordinary 2-second recovery tick, verified Heavy Attack completion, and caller-proven additional resource maximum/restoration event timestamp. Extra boundaries are safe; missing external event families fail closed.
- **Late finalized potion timing axis** — generated Objective #32 search no longer gives canonical `potion_timing_policy` ownership to the early anchored rotation axis. The early generated axis searches delayed Ultimate timing only and selects the explicit `potion:none` slice. Canonical potion timing is enumerated after execute and Heavy Attack policies from finalized DD + resource observation evidence, including no-use, exact boundary ordering, and open-interval representatives.
- **Exact-leaf final progression authority** — generated exact simulation now consumes the final assembled `CharacterProgression` rather than the pre-late gear-stage progression, ensuring searched passive-rank mutations actually reach Combat Simulation. When the finalized-potion axis is present, its plan supersedes the earlier runtime-policy plan at the leaf.
- **Scheduled potion restoration evidence** — exact POTION actions can now resolve source-backed `PotionUseEvent.instant_restores` into ordinary Phase 4 `ResourceRestorationEvent` rows. Magicka, Stamina, and Health restores are emitted from sourced potion-tier magnitudes, candidate identity is preserved, mismatched potion names fail closed, and Phase 4 continues to own resource filtering, caps, waste, and timeline ordering.
- **Same-tree canonical coverage promotion** — once the generated branch-and-bound tree proves its finite denominator maximum, Objective #32 can derive canonical axis coverage directly from that exact tree’s axis inventory. Physically absent axes cannot be rescued by unrelated external proofs, and axis-carried theoretical omissions such as anchored potion timing or reviewed-only Heavy Attack windows remain attached automatically.
- **Generated rotation-family action-count proof** — derives the seed-family maximum damage-action count directly from the canonical semi-static step cycle, exact horizon, legal start-bar routes, and weave state without constructing every skill-order permutation. Later policies that can add damage actions, primarily Ultimate insertion, must provide a separate proven additional-count ceiling before the broader descendant branch can reuse the proof.
- **Sustained-DPS Ultimate added-action count proof** — counts the exact scheduled selected-bar Ultimate actions in one fully materialized generated policy and requires final canonical resource legality before promoting that count as a safe added-action ceiling. No-Ultimate proves zero; illegal or unresolved policies stay open.
- **Phase 14 sequential target-Health feedback** — evaluates DD damage actions in exact time/sequence order against an execution-local target Health ledger, then exposes that evolving Health through canonical CombatStateSnapshot evidence so later execute/target-Health-sensitive actions can react to damage dealt earlier in the same simulation. The simulator supplies Health truth only; execute thresholds and amplification remain owned by existing Rotation DD mechanics.
- **Phase 14 fight termination projection** — stops evaluating later DD damage once the simulated target reaches zero Health and truncates the executed RotationPlan at the exact killing action so downstream resource/healing/effect projections use the actual fight horizon rather than continuing after target death.
- **Phase 14 occurrence-level periodic damage** — Combat Simulation consumes exact-time damage occurrences from the canonical Rotation DD skill evaluator. Mixed direct+DoT skills preserve the cast-time hit and later reviewed tick timestamps separately; snapshot-at-cast and dynamic-at-tick magnitude policies reuse existing canonical math, successive-hit scaling stays occurrence-indexed, and whole-plan DoT totals are never front-loaded at cast time.
- **Phase 14 same-instant damage ordering guard** — if a periodic tick and a scheduled damage action share an exact timestamp and no reviewed cross-source ordering rule exists, the DD simulator stops the damage timeline at that boundary and reports the ambiguity instead of silently choosing list order. Earlier strictly ordered damage remains valid.
- **Phase 14 Health chronology guard** — per-recipient damage/healing events that share the same timestamp, priority, and sequence now fail closed instead of inheriting alphabetical event/source ordering. Earlier proven Health history remains valid, later Health projection for that recipient stops at the ambiguous boundary, and snapshots can resolve an exact same-time sequence boundary.
- **Phase 14 same-source component coalescing** — multiple damage components from the same source/event type at the same simulation coordinate remain separate raw audit events but are combined into one Health consequence before overkill/death math, making Health results independent of component insertion order while preserving damage-detail visibility.
- **Phase 14 causal event ordering** — raw damage/healing consequences precede derived Health changes, which precede death transitions; the event queue no longer compares payload contents to break ties, and equal semantic queue keys preserve deterministic insertion order only as a technical fallback. Post-death damage is suppressed instead of emitting meaningless 0→0 Health changes.
- **Phase 14 sequence-complete snapshots** — exact-time sequence boundaries now apply consistently to bar state, resources, Health, and active effect windows, while the legacy sequence-less snapshot still means end-of-timestamp state.
- **Phase 14 deterministic replay verification** — runs the same deterministic simulation twice and compares the canonical result signature field-by-field, surfacing drift in events, resources, effects, target state, bars, duration, or unresolved evidence rather than merely assuming replay stability.
- **Phase 14 damage-specific completeness** — Combat Simulation now tracks damage-blocking unresolved evidence separately from general simulator gaps. Damage summaries report attempted/applied damage, overkill, killing source, and per-source kill attribution, and modeled DPS is withheld only when damage-relevant evidence is unresolved rather than for unrelated non-damage limitations.
- **Phase 14 real saved-build DD simulation audit** — generates a real saved DD RotationPlan, rebuilds exact plan-owned attacker state (bar, scheduled potion, reviewed persistent toggles), runs canonical skill/LA/HA/Ultimate and occurrence-level periodic damage through Combat Simulation against explicit target Health/resistance/state assumptions, summarizes source damage/ending Health/kill time, and withholds modeled DPS whenever unresolved damage evidence remains.
- **Phase 14 explicit death state** — emits deterministic death transitions when explicit Health reaches zero, exposes `is_dead` in exact-time snapshots, and blocks implicit healing-based resurrection until revive semantics are modeled.

- **Phase 12.5 canonical closeout audit** — a read-only real-data audit checks persisted Raid Plans against reusable saved Builds and Roster identities, preserving recruit/open chairs, stable player/character/build IDs, locked class/role/gear choices, assignment provenance, explicit unresolved state, temporary-only persistence round-trips, and Optimizer Adviser non-mutation.

This file is a working index of features, tools, workflows, and notable user-facing capabilities that exist in BFF / FoundryDock.

Its purpose is simple: **do not forget the useful, strange, ambitious, or unexpectedly nice things we build.**

This is **not** the development roadmap and should not be used as the authority for phase completion. See `MASTER_ROADMAP.md` for implementation status, validation requirements, and planned work.

When a new feature becomes usable or a meaningful capability is added to an existing feature, add a short entry here.

---

## App / General

- Desktop application
- Executable packaging for local distribution
- Friend/shareable executable builds
- Optional executable builds that exclude the Broadcast module
- Application update support
- Release-gated executable packaging with a positive asset allowlist instead of bundling the entire repository asset tree
- `RELEASE_STATUS.md` tracks working, in-progress, disabled, legacy/deprecated, and runtime-required release boundaries separately from the broader feature index
- `packaging/release_manifest.py` is the machine-readable release payload contract for approved assets, database seed data, clean first-install state, user-owned state, and forbidden legacy asset trees
- `tools/audit_release_candidate.py` validates the packaging boundary and can block final builds while runtime data files remain unclassified
- `packaging/build_release.ps1` builds versioned first-install packages and updater payloads only after the strict release audit and test gate pass
- Release updates preserve the live `eso.db`, settings, builds, roster/progress/session state, and other user-owned data rather than replacing them with developer copies
- Clean first-install releases create an empty canonical characters catalog so developer/test player identities are never shipped in a fresh package
- `app_version.py` remains the single source of truth for the application release version
- Multiple visual themes
- Theme-aware UI components
- Rylo theme and Rylo-specific visual assets
- Theme-aware cards, controls, tables, and result surfaces
- FoundryCard headings resolve semantic heading icons from the canonical `assets/icons` library before compatibility icon locations
- Semantic icon lookup accepts space, hyphen, underscore, singular/plural, and reviewed alias variants so theme-aware UI surfaces can use one meaning-oriented icon vocabulary without leaking missing filename text into card headings
- BFF sidebar branding uses the compact **BFF / RAID OPERATIONS** lockup and compass mark
- Sidebar top-level groups are ordered **Raid → Team → Build → Encounter → Review → Achievement → Collectibles → Tools → Settings** while preserving the existing child destinations within those groups; optional Broadcast tools stay inside Tools instead of creating another top-level section
- Urban Wilderness uses a single low-stimulation, red/green-independent raid-lead workflow with static artwork, no flashing UI effects, Roster as the landing surface, and Community News hidden while disabled
- **Live Raid field-console styling** — the existing Live Raid cards use the compact nocturnal dispatch treatment from the approved mockups: shallow warm-gold card borders, blue-black interiors, compact status tiles, flattened Raid Spots table, denser Callouts / Recent Events / Coverage surfaces, a shallow Next 60 Seconds panel, and parchment reserved for Quick Notes / Run Sheet. Styling never fabricates telemetry: roster count remains planned state, phase remains planned context, and observed state stays explicitly unavailable until a real runtime source exists.
- Raid Engine overview accents and New Build entry adapt to Foundry teal/amber or Rylo's squared steel palette, with text and numeric progress cues alongside color
- Main overview keeps four cards in each dashboard row at desktop widths; compact labeled attribute meters use red Health, green Stamina, and blue Magicka in both visual themes
- Current Gear also lists the active profile's bookmarked sets, keeping the main dashboard's detail and goal rows at four cards each
- Main and Coverage share canonical saved-build effect evidence: available static sources, conditional sources, effects not identified, and unmapped or unaudited effects remain distinct; Coverage filters work without inferring assignments or uptime
- Coverage is **saved-Raid-Plan scoped** in the Raid workflow: its visible selector lists only persisted trial-specific Raid Plans, including trial and difficulty context. Reusable Roster teams and library-wide saved-build audits remain lower-level/internal capabilities and are not presented as competing Coverage scopes. Raid Plan planned gear can contribute reviewed set-only Conditional coverage evidence without pretending a full Saved Build exists.
- Raid Engine navigation opens Coverage without injecting transient Team Optimization or Roster-team state; provider evaluation follows the selected persisted Raid Plan as the trial-specific source of truth.
- Raid Engine dashboard summarizes static coverage evidence with distinct available, conditional, not-identified, and unverified labels rather than treating an unknown effect as missing
- Raid Plans provides a visible 12-chair trial-planning workspace where gamertag can be known before character, role, class, or build, and saved builds can be assigned without mutating existing Roster persistence
- Raid Plans now keeps the non-editable center note as a static quote panel, removes the redundant top Roles/Spots field-note card, and routes detailed assignment work to the dedicated Assignments surface
- Raid Plan footer keeps only the blue primary Save action on the right; cross-page tools are reached through normal navigation instead of footer handoff buttons
- Assignments separates buff/debuff ownership from utility/mechanic jobs, combines primary/secondary support responsibilities into one visible column, moves notes into Selected Spot, and shows Plan Snapshot instead of a Mechanic Coverage link card. Each chair also has a small free-text **Source** annotation for planning shorthand such as `WW`, class skill, or proc set; it is display context only and never substitutes for the actual buff/debuff assignment.
- App navigation uses a shared unsaved-change contract: editable pages that report pending changes receive Save / Discard / Cancel before page navigation
- Raid Plans can show trial-specific wide hero artwork for Cloudrest, Sunspire, and Dreadsail Reef; banners resolve from the selected plan identity, crop without distortion, fail closed for unmapped trials, and are bundled as compressed WebP release assets
- Comp Builder now owns canonical `CompPlanState` **before or after Raid Plan creation**. Team/Roster/Assignments intake can create an unbound planning state directly; Save finalizes it into a new non-colliding Raid Plan and rebinds the same Comp session, while a Raid Plan-bound session updates that same plan. Supported Phase 14 save does not require generated-roster drafts, create Personnel, or overwrite reusable saved Builds/Roster membership.
- Phase 14 Comp Maker has route-independent canonical ownership: direct-open sessions, Roster/Assignments handoffs, and existing Raid Plan handoffs all operate on `CompPlanState`. Loaded Raid Plans finish hydration clean; unbound planning supports Save/Discard and **Send to Raid Plan** through the same canonical persistence path.
- Phase 14 Comp Maker saved-plan switching is unsaved-change safe: selecting a different Raid Plan from the editable plan picker now uses the same **Save / Discard / Cancel** contract as page navigation, restores the current plan selection on cancel or failed save, and never silently replaces dirty canonical Comp state.
- Phase 14 Comp Maker persistence now has one supported runtime authority: `CompPlanState` → canonical Comp Builds → Raid Plan. The retired generated-roster draft service, generated-draft-to-Raid-Plan bridge, and local composition-template persistence are no longer constructed or callable by the Comp Maker runtime; compatibility stubs remain only where decorator wiring still expects the old method names.
- Phase 14 Comp Maker selected-chair setup provides a searchable **full Gear Catalog** assignment control. Raid leads can add or remove planned sets directly from canonical Comp state; smart recommendations remain optional suggestions and no longer define which gear is accessible.
- Phase 14 Comp Maker memoizes canonical Team Health, selected-chair proposal analysis, and selected-chair candidate discovery by immutable planning inputs so ordinary UI repaint/selection signals do not repeat expensive whole-team evaluation.
- Phase 14 Comp Maker never performs a live ESO Logs network request during application startup or ordinary trial selection. Those paths refresh local roster/reference candidates and clear stale trial observations; current ranked-team evidence is fetched only through the explicit **Refresh Build Sources** action, so an unavailable or slow ESO Logs response cannot prevent the application window from opening.
- Phase 14 Comp Maker selected-chair planning exposes compact **Primary Coverage**, **Backup Coverage**, and free-text **Source** controls backed by canonical assignment state, so effects such as Crusher can be planned directly without a mechanic-specific widget. Existing `planned_skills` are visibly summarized in the Why panel with the full skill list on hover, even when no recommendation candidate exists.
- Phase 14 Comp Maker **Fill Empty Skills** can seed an empty chair skill plan from a saved build with known skills or a complete reference template. It never replaces existing `planned_skills`, respects the Skills lock, and keeps incomplete references plus observed ESO Logs abilities as suggestion/evidence only. Generic recommendation Apply continues to leave skills untouched.
- Phase 14 **Selected Chair Adviser** compares a candidate against canonical `CompPlanState` before applying it, showing planned coverage gained/lost, assignment-evidence improvements/regressions, reviewed source-evidence changes, duplicates added/removed, and independently preserved lock fields. The Apply action uses the same proposal service as Preview for Raid Plan-bound sessions.
- Phase 14 Comp Maker is **assignment-aware composition planning**: bulk Fill from Roster / Auto-Fill operates on canonical `CompPlanState` to fill unresolved chair/build decisions while preserving explicit/locked choices, consuming each real saved player at most once, preferring canonical player/character/build identity, and treating explicit Primary/Backup responsibilities from Assignments as hard provider requirements for that chair. Comp Maker chooses a compatible build/gear/skill package where evidence proves the assigned effect; it does not invent new responsibility ownership from generic missing Team Health rows. Team Health remains visible for missing/duplicate composition feedback, while Optimizer improves an already valid saved plan afterward.
- Phase 14 **Team Optimization Recommendation Workbench** opens one exact saved Raid Plan as a read-only 12-chair snapshot and ranks canonical plan blockers, missing providers, conditional execution, redundant providers, and Foundry evidence debt. Recommendations expose current/proposed review state, affected providers, tradeoffs, confidence, and honest coverage/build-resolution summaries; unsupported survival, sustain, uptime, and raid-DPS projections remain visibly unevaluated. Review selection never overwrites a Build or Raid Plan, and scenario saving stays disabled until a canonical reversible proposal model exists. Its scope banner distinguishes no handed-off plan from a loaded empty or partial team and explains that incomplete teams remain valid review input while open chairs are reported as blockers. Normal application startup now constructs only this lightweight Phase 14 workbench: the retired editable Optimization page, generated-roster draft bridge, legacy canonical-analysis presentation, and Optimization provider-workload widgets are not initialized at launch. Saved-build loading, capability resolution, and the reusable Raid Plan Adviser service are lazy-created only when `set_raid_plan_adviser_scope` receives an exact saved Raid Plan. `console:6` remains the route identity, and `tools/profile_team_optimization_startup.py` records constructor time separately from first plan-scope analysis without writing user data.
- **Coverage planning semantics:** Raid Plan Coverage answers whether the planned group has a source for each tracked buff/debuff. Planned gear, reviewed planned skills, reviewed class-passive sources with their planned trigger skill line, conditional/proc sources, and explicit provider assignments count as **Covered** for planning; self-only skill effects are excluded. Evidence labels separately describe Supported, Conditional, Planned/runtime-unproven, duplicate ownership, or Missing. Rotation and Raid Review remain responsible for execution and observed uptime.
- Phase 14 Comp Maker Team Health **Covered** tile exposes its current default-required buff/debuff checklist on hover, sourced from the same canonical Team Health requirement list rather than duplicated UI text.
- Phase 14 Comp Maker Team Health now reads the canonical `CompPlanState` for bound and unbound planning sessions rather than reconstructing coverage from presentation tables. Planned gear and reviewed planned skill/class sources use the same planning-coverage semantics as Coverage, including perfected-set family normalization such as `Perfected Roaring Opportunist` resolving through the reviewed `Roaring Opportunist` provider relationship; explicit primary/backup assignments remain raid-lead intent and count as planned coverage while proof strength stays separately visible. Duplicate effects, genuinely missing required effects, open player seats, and open gear decisions are derived from the state that Save will persist.
- Readiness parchment notes use fixed-height field-journal pencil/sketch artwork; full-color city art is reserved for dark surfaces and decorative art cannot change page/card geometry
- Empty boss, mechanic, positioning, and Raid Map panels use distinct compact field-art placeholders for Foundry and Rylo, with visible labels that distinguish decorative art from reviewed encounter evidence
- Main character overview dashboard
- Role-aware application surfaces
- Archive page / archived data access
- Startup defers heavy independent pages including Achievements and the Collectibles category browser until first use; the visual Collectibles dashboard and Rotation Builder remain eager, and the lazy Collectibles browser reuses the dashboard's shared profile-aware service

---

## Character & Build Management

- Character records
- Multiple saved builds per character
- Separate Character and Build entities
- Saved build loading and editing
- Canonical build representation
- Character-owned progression separate from individual builds
- Build identity persistence
- Race selection
- Class selection
- Attribute configuration
- Gear configuration
- Armor traits
- Armor enchantments
- Jewelry traits
- Jewelry enchantments
- Front-bar and back-bar weapon configuration
- Skill selection
- Morph selection
- Ultimate selection
- Passive skill ranks
- Owned skill-line tracking
- Champion Point configuration
- Mundus selection
- Food selection
- Potion selection
- Scribed Skill support
- Build readiness / progression checks
- Character Progression interface
- Skill-passive purchasing controls
- Bulk **Buy All** controls for progression sections
- Saved-build comparison is available in Rotation Builder's generated **Compare** workflow; full encounter-aware build comparison remains future optimization work
- Build-linked performance evidence is surfaced through the Performance Dashboard rather than a separate Builds performance panel
- Save generated or modified builds for later reuse
- **Copy Build To...** creates an independent same-class build for another canonical character without copying player identity, character progression, team assignments, or Ready state
- **Save as Template** stores reusable role-level build setup with explicit class overlays; Templates are browsable from the Builds view and can be applied to another character
- Cross-class template application keeps shared role setup but fails closed on class/skill state when no matching class overlay exists, leaving those slots for review instead of guessing

---

## Build Import

- Screenshot/OCR-assisted build and character intake implementation is retained, but its user-facing control is temporarily disabled while raid-planning intake ownership is being stabilized
- Raid roster import from Excel workbooks, CSV, and JSON with a review preview before persistence
- Raid workbook detection supports player-specific multi-loadout sheets and sectioned team sheets with shared role/build templates
- Imported builds are matched to gamertags and saved through the canonical Player -> Character -> Build catalog only after character identity is resolved or confirmed
- Achievement progress import

---

## Exports & Sharing

- **Finch operational sync** uses the configured hosted Finch URL/API credential to explicitly pull pending team gear-needs into existing FoundryDock Personnel assignment state. Sync is user-triggered from Assignments, runs off the UI thread with a worker-owned SQLite connection, resolves only exact current gamertags or explicit aliases on an existing team, rejects archived/ambiguous/unknown identities instead of guessing, and acknowledges Finch only after the local write succeeds.
- **Finch shared publishing** exposes explicit nonblocking Publish Team and Publish Raid Plan actions. Shared Team schema v1 contains Team name, saved schedule/current focus, and basic active roster identity; Shared Raid Plan schema v3 contains stable plan identity, trial/difficulty/status, Team name, seat/player/character/class/role outline, explicit primary/secondary/utility assignments, and a lightweight build summary (build name, source label, planned gear sets, planned Mundus). Schemas v1 and v2 remain readable for backward compatibility. Personnel notes, Discord URLs, local database ids, full build payloads, plan notes, and chair notes remain local by design.
- **Finch Collaboration overview** adds one read-only Team Operations page that lists shared Teams, Raid Plans, Readiness, and Coverage together with publisher, UTC update time, provenance/status, summary counts, and an **Open Workspace** handoff. A compact attention summary and filters surface **Changed Since Copy**, **Not Copied**, **Readiness Gaps**, and **Coverage Gaps** from structured state rather than display-text parsing. When provenance supplies an exact local copy key, **Open Workspace** restores that precise local Raid Plan in Raid Plans, Readiness, or Coverage; uncopied snapshots fall back to the owning workspace without manufacturing local identity. The overview never publishes, imports, or mutates local state; refresh remains an explicit user action.
- **Finch collaboration provenance** records which Finch snapshot produced each explicit local Team/Raid Plan copy in a local sidecar, then shows publisher, UTC update time, and honest copy status (**Not copied locally**, **Copied from this Finch snapshot**, or **Updated on Finch since copy**) in shared pickers. This compares Finch against the last copied remote snapshot only; it does not pretend to know whether later local edits are newer.
- **Finch shared Coverage** publishes a separate schema-v1 operational snapshot for one saved Raid Plan: each raid-facing effect's covered/missing state, evidence label, primary/backup ownership, static/conditional provider labels, duplicate-primary flag, review-needed flag, aggregate counts, and unresolved-chair count. Shared Coverage is viewed read-only and never mutates the local Raid Plan, Coverage state, Personnel, Builds, assignments, notes, or runtime evidence.
- **Finch shared Readiness** publishes a separate schema-v1 operational snapshot for a saved Raid Plan: per-chair build state, assignment presence, Coverage state, and explicit Human Ready state plus aggregate counts. Shared Readiness is viewed read-only and never mutates local Human Ready flags, Raid Plans, Personnel, Builds, run notes, or local identity.
- **Finch shared retrieval / Copy to Local** exposes explicit **Get Shared Teams** and **Get Shared Plans** actions. Finch snapshots remain read-only previews until the user explicitly copies one locally. A shared Team copy creates a new non-colliding local Team containing schedule/time zone/current focus only and never creates Personnel; a shared Raid Plan copy creates a new non-colliding local Raid Plan outline, preserves explicit primary/secondary/utility assignments plus lightweight build summaries, and never creates Personnel, Characters, saved builds, or local build identity. Shared snapshots never overwrite matching local Team names or Raid Plan ids.
- Finch shared snapshots are versioned and replace by stable Team/plan key on the hosted service; named API clients require `shared:read` / `shared:write`, while the legacy owner key remains backward-compatible through its wildcard scope.
- Finch remains an operational companion rather than local-data authority: it does not create Personnel/Teams during gear sync and it does not receive the local `eso.db`, draft builds, private Personnel notes, or unrelated local application state.
- Neutral CSV exports for spreadsheet / structured-data interchange
- Theme-aware human-facing PDF exports for Builds and Roster
- Build / roster export workflows
- Discord-formatted roster sharing
- Shareable local app builds

---

## Character Overview Dashboard

- Character summary / overview
- Build summary information
- Achievement progress overview
- Completed-achievement counts and category progress for the selected profile
- Sticker Book and Mount ownership totals from saved profiles
- Raid schedule panel
- Pinned Performance Focus goals
- Saved Gear Lookup bookmarks by profile
- Ready checkbox on each saved build in the Phase 14 Builds inspector; the saved readiness state is also reflected on the Raid Engine Overview
- Saved-build static capability evidence with unverified and conditional states
- Active expedition and encounter labels refresh with the overview
- Compact visual progress indicators

---

## Builds Editor

- Edit canonical saved builds
- Character progression management
- Skill and passive ownership controls
- Champion Point configuration
- Gear and set editing
- Weapon-bar configuration
- Skill-bar configuration
- All Build Editor dropdowns use searchable autocomplete combo boxes with case-insensitive contains matching and clear controls
- Mundus / food / potion configuration
- Scribed Skills interface
- Build-linked performance context is available through the Performance Dashboard; the Builds inspector itself remains build/configuration focused
- Role-aware build information
- Context Variants provide sparse **Team**, **Boss**, and **Team + Boss** gear, weapon, Mundus, Champion Point, skill-bar, food, potion, and note overrides while unchanged fields inherit automatically
- Matching build variants resolve field-by-field as **Team + Boss -> Team -> Boss -> base build**, so a boss-specific tweak does not require cloning an entire build
- Conditional **Rotation** tab appears only when the selected build owns a saved completed rotation
- Phase 14 Builds command center provides first-class All, Mine, Team, Comp Builds, Templates, Favorites, and Archive library views with compact search/class/role/content/source filtering and a persistent inspector area
- Phase 14 Builds navigation is canonical-id aware and filter-proof: Readiness opens the exact saved `BuildId`, Comp-origin builds automatically surface in **Comp Builds**, and explicit player/build handoffs clear stale search/class/role/content/library filters before rendering the requested target.
- Phase 14 Build Edit preserves canonical identity and non-editor state across saves, including `PlayerId`, `CharacterId`, `BuildId`, `BuildKind`, Comp source-plan/seat provenance, planned gear/skills, context variants, subclass state, second Mundus, transformed form, and full scribing recipe state.
- Phase 14 Comp Builds now surface planned Comp Maker state directly in both focused and full Build editors: planned set packages, planned skills, Raid Plan source, and seat remain visible even before exact gear slots or skill-bar positions are materialized.
- Phase 14 Build library/profile identity now prefers explicit canonical `BuildId` over display-name re-resolution; stale explicit IDs fail closed instead of silently rebinding. Build Copy and Templates intentionally strip player/character/build identity plus Comp plan provenance and planned-provider metadata so reused builds enter the catalog as new ordinary saved builds.
- Raid Plan now exposes **Share Builds** for the plan's exact resolved canonical builds: printer-friendly white-background PDF, CSV, and Discord clipboard text. The PDF deliberately avoids dark fills, artwork, textures, and decorative backgrounds to minimize ink use.
- Phase 14 Mine/Team library views are now actionable profile filters: build ownership and optional source owner are editable additive metadata keyed by canonical `BuildId`, with no duplicate build store and no rewrite of the saved build payload.
- Build Favorites and Archive state are persisted as additive profile metadata keyed by canonical build id, so starring or archiving never duplicates or deletes the underlying saved build
- Build-level inherited gear baseline defaults to **Gold · CP160 · Truly Superb**; blank item values inherit the baseline while existing explicit values are preserved and differing values are counted as visible exceptions
- The Phase 14 inspector uses Overview, Gear, Skills, CP, Progression, Consumables, Scribing, and Notes tabs, with Gear grouped into Armor, Jewelry, Front Bar, and Back Bar summaries instead of one giant spreadsheet
- The Phase 14 library reasserts the approved library-left / inspector-right split after legacy Builds decorators finish, restores the selected-build dossier when older wrappers detach it, and presents only one visible New Build entry while continuing to route through the canonical Easy Mode creator
- Phase 14 Build polish moves the single **Create New Build** action beside Help as a solid-gold primary control, adds class icons and colorblind-safe role icons, decorates equipment/food/potion rows with the canonical icon library, and restores canonical ESO ability artwork to the Skills inspector cards
- Heavy canonical editors remain lazy from the command-center view: the outer editor tabs are hidden while browsing and appear only when Edit, Character Progression, or Scribed Skills is explicitly opened
- Build Edit remains the canonical inline editor, but **Save** and **Cancel** return directly to the Phase 14 Builds library/inspector instead of leaving the user stranded on the legacy Edit workspace
- Focused Build editors open only the requested section (identity, Armor, Jewelry, Front Bar, Back Bar, Skills, Champion Points, Consumables, Notes, and existing Scribing access) from the right-side dossier; the monolithic legacy editor is retained only as a compatibility fallback
- The Phase 14 right-side dossier uses compact grouped Gear cards, slot/class/role/consumable icon vocabulary, collapsible per-slot detail, a compact baseline/exception strip, More Actions menu, and a single gold Save action
- Base skill bars and Base Champion Points are labeled explicitly in the Phase 14 dossier; Team/Boss context variants inherit those base values until a sparse override is recorded
- The Phase 14 Overview visibly summarizes saved **Context Variants** and provides direct access to the canonical Team / Boss / Team + Boss variant editor instead of hiding it behind the legacy-editor fallback
- Build Gear popups and dossier summaries use the reviewed ESO trait icon vocabulary for weapon, armor, and jewelry traits while retaining the trait text as the accessible source of meaning
- Phase 14 uses a user-extensible semantic icon resolver that tolerates spaces, hyphens, underscores, capitalization, and localized Windows filename display quirks, then reasserts Builds icons at the final visible-page boundary

---

## Rotation Builder

- **Owned Phase 14 runtime** — Rotation Builder is registered through a single page-owned UI boundary rather than the legacy patched dashboard; safe direct integrations include recovery Heavy Attack stabilization from reserve pressure, required-effect Heavy Attacks, duration-aware visual timeline/uptime lanes with ability icons, reviewed encounter-demand visibility, current-vs-saved comparison, build-owned rotation persistence, saved notes/context metadata, and PDF export.
- Phase 14 keeps Rotation Builder as its own workspace, separate from Builds, while accepting the selected saved build as input
- Pre-generation command center exposes **Safe Progression**, **Balanced**, and **Maximum Output** intent presets over the existing canonical rotation controls rather than inventing a second planner state
- Preset-selected execution/sustain values remain visible, editable, show a **Customized** state after manual changes, and can be reset to the active preset defaults
- Rotation context is summary-first: the collapsed bar emphasizes character/build/trial/boss/difficulty, while Team remains available under **Edit Context** for assignment-sensitive generation
- Phase 14 Rotation uses the canonical theme-aware `assets/icons` vocabulary across context chips, intent presets, generated settings, obligation rows, advanced controls, and result navigation; larger intent/settings/obligation/action surfaces fill the two-column workspace instead of leaving the command center top-heavy
- Phase 14 Rotation polish adds visible labels beneath the context values, enlarges the intent/setting/obligation surfaces, makes Generate Rotation the solid-gold dominant action, suppresses redundant legacy header actions during setup, and keeps icon-led result navigation below the command center
- Build skills, gear procs, team duties, pressure windows, and advanced rules are presented as compact obligation summaries; unsupported evidence remains explicitly unresolved instead of being fabricated
- Timeline, Uptime & Resources, Explanations, Compare, and Save & Export are exposed as an icon-led result navigation row beneath the setup cards and remain disabled until a generated plan exists; the canonical result tabs stay available after entering a result view
- The Phase 14 Rotation page now owns its visible widgets and signals directly; the legacy patched dashboard is not constructed, avoiding deleted-Qt-label and repeated-reparenting failure paths.
- Context-chip polish is idempotent across repeated page visits, so reopening Rotation Builder does not stack duplicate Character/Build/Trial/Boss/Difficulty captions
- The Rotation Builder front page is constrained to four primary setup surfaces before generation: compact Context, Rotation Intent, Inputs & Obligations, and the result-navigation panel; intent icons are displayed above their labels and generated-setting rows expose explicit edit pencils
- Rotation Intent, Inputs & Obligations, and Generated Settings use the large gold section hierarchy from the Phase 14 mockup, while result navigation and semantic SVG icons are reasserted at the final visible-page boundary
- Roster > Characters portrait circle is editable: bundled defaults are discovered from `assets/avatar`, custom portraits are copied to user-owned `data/avatar`, and the selected avatar is persisted on the canonical character record rather than in `eso.db`
- **Generate Rotation** remains the single dominant setup action; save/export controls stay in the generated-result workflow

---

## Performance Dashboard

- Player performance dashboard
- Role-aware performance layouts
- Healer performance view
- Damage Dealer performance view
- Tank performance view
- Performance data loading from saved player/build context
- Role-specific metric visibility
- Group support / buff analysis
- Role-aware Performance Focus recommendations and pin-able improvement goals; Raid Engine Overview also exposes its Skills to Work On summary
- Build-linked performance context and ESO Logs build evidence

---

## Combat Reference

- Searchable combat-reference workspace
- Source and entry-type filtering
- Player-facing common mechanic names / raid callouts are displayed and searchable without replacing canonical mechanic identity
- Short reviewed **How to Mitigate** guidance gives a large, scan-friendly survival instruction for supported mechanics
- Human-readable gameplay-practice entries sourced from the shared gameplay-policy registry
- Canonical encounter-mechanic entries sourced through the shared encounter repository / projection
- Canonical combat-effect entries sourced read-only from `combat_effect`, `combat_effect_trigger`, and `combat_effect_interaction`
- Canonical gear sets, active skills, passives, and Champion Points are exposed as searchable Reference Data entries
- Gear Lookup strips ESO client and HTML-style color markup from set names, categories, acquisition/source text, and bonus descriptions; existing Gear Lookup rows in `eso.db` are cleaned in place on refresh, and future gear-set imports are normalized before persistence.
- Skill and passive entries include their skill line in the display identity so same-name abilities do not silently collide
- Related boss / encounter names include their parent dungeon or trial when known, including reviewed dungeon identities whose raw boss import is missing
- Reviewed gear / skill / passive / Champion Point version history can be shown as a **History / Legacy** flavor section with source provenance
- Reference history is presentation-only trivia and is never consumed by combat math, rotation, optimization, provider resolution, or canonical mechanics runtime
- Status effects expose duration, tick interval, stack limit, immunity duration, triggers, and effect interactions when present
- Reviewed research enrichment adds provenance-bearing values from official patch notes and corroborating sources without silently promoting them into combat-math authority
- Core U41+ status-effect entries now include useful delivery and secondary-effect details for Burning, Chilled, Concussion, Diseased, Hemorrhaging, Overcharged, Poisoned, and Sundered
- Off Balance exposes official player-source duration, reapplication lockout, and non-consumption behavior
- Dreadsail Reef Hindered, Rattled, and Devitalized entries expose reviewed encounter context and debuff behavior with explicit confidence/provenance
- Bare `Not modeled` values are replaced in the normal Reference loading path by specific evidence-state language describing exactly what remains unknown or unreviewed
- Related effects are derived from explicit effect-interaction rows rather than inferred from prose
- Canonical Major / Minor named-effect entries sourced from `minmax.named_combat_buffs`
- Named-effect entries expose U50 standing semantics and explicit U51 semantic differences without mutating the U50 default
- Component-owned effects such as Vulnerability, Protection, Berserk, Slayer, Vitality, and Defile remain labeled as component-layer semantics instead of receiving invented standing-stat values
- Named-effect stacking guidance follows the canonical rule that duplicate copies of the same named effect/objective do not stack while Major and Minor variants remain distinct
- Reviewed ability providers are sourced read-only from the `ability_combat_effect` relationship table and enrich matching effect entries with ability, relationship, weapon, condition, confidence, and provenance
- Ability-provider identity uses canonical lower-snake-case `index_name` values; duplicate numeric ESO ability aliases are collapsed and never promoted to semantic identity
- Reviewed gear-set providers are projected from `minmax.gear_set_known_effects` through canonical gear-set rows without parsing bonus-description prose
- Versioned potion-trait providers expose U50 / U51 Alchemy named-buff mappings from `minmax.alchemy_potion_buff_semantics`
- Named-effect entries explicitly mark passive-provider coverage unresolved until one shared reviewed passive-provider authority exists
- Provider relationships are shown only when explicit source-backed mappings exist; missing provider mappings remain absent rather than being inferred from tooltip prose
- Encounter entries expose only structured known fields such as mechanic type, damage type, target count, movement / positioning / cleanse requirements, hazard state, fatal-failure state, interruptibility, phases, and provenance
- Structured role, content, default-behavior, confidence, exception, and modeling-requirement details
- Related-concept links and cross-reference text
- Evidence / provenance display per reference entry
- Per-entry death-review guidance
- **Used By FoundryDock** display showing which systems consume or are affected by the referenced rule
- Mechanic / attack visual placeholder retained for future artwork, combat-log samples, and positioning diagrams

---

## Roster

- **Teams decorative art** — the Teams lower-left panel uses the Urban Wilderness full-color night rectangle in a fixed-height crop; artwork cannot expand or reshape the surrounding workspace/cards.

- **Player avatar picker** — clicking the Character detail portrait opens the bundled `assets/avatar` choices; the selected portrait is stored on the canonical Player and follows all of that player's characters/builds.

- Player roster
- Character roster records
- Role assignment
- Build assignment
- Multiple builds associated with roster characters
- Load roster players into team workflows
- Preserve roster identity separately from build identity
- Save generated team builds back to roster characters
- Roster summary cards for Players, Characters, Teams, Availability, Recruitment, and Archive open their detailed workspaces inline beneath the card bar instead of launching modal pop-ups
- Roster top cards use stable fixed geometry, semantic badge/icon treatment, and the existing Collectibles number-sheet contract without allowing artwork to resize the page
- The active Roster dashboard retires the old raven/street filler panels entirely; full-color art is not placed on parchment surfaces
- Players keeps a visible people table beside the editable Player Record instead of opening a form-only detail view
- Character detail uses a compact profile-style panel for player, class, race, role, teams, status, and saved-build count without showing Builds or Assignments tabs
- Assignment **Selected Spot** uses a structured profile card for player, character, role, primary/secondary duty, gear status, linked build, and notes with direct Build/Rotation/Edit Duties actions
- Import external raid rosters by merge rather than replacement, preserving unrelated players, teams, characters, and saved builds
- Imported build/team assignments are attached to the canonical gamertag/character/build identities when the import can resolve them uniquely
- Personnel keeps explicit **Known Aliases** for old gamertags, Discord names, and raid-sheet names; aliases are learned from manual entry, renames, or explicit merges rather than guessed from similarity
- Personnel stores optional **Discord**, **YouTube**, and **Twitch** identity fields alongside the Xbox gamertag; duplicate-player merges preserve them.
- Personnel includes **Import Discord Screenshot…** for low-friction intake: the screenshot is shown beside Xbox/Discord/YouTube/Twitch fields, fills only the current Player form, and never saves automatically.
- **Merge Players…** consolidates two user-confirmed Personnel identities, preserves their teams and assignment state, moves canonical characters/builds under the kept player identity, creates backups, and remembers discarded names as future import aliases
- Future roster imports reuse exact learned aliases as player identity evidence, including all canonical characters already known for that player
- Assignments persist separately for each **roster member + team**, so one character can have different normal jobs on different teams
- **Comp Builder roster-first planning** keeps imported/selected groups at their real ESO size (4 or 12), shows Player from first paint with empty seats labeled `Recruit`, accepts **Load Team** directly on the page, treats Recruit as an open prescription slot that cannot borrow another player's saved build, scopes saved-build choices to the loaded player, and preserves known player/character identity when a build decision is still unresolved; the approved Phase 14 Work-chat shell is organized as **Raid Brief -> Recommended Team Plan + Why This Plan -> Team Health**, with Generate Team Plan as the dominant action and legacy matrix/evidence infrastructure kept behind the visible workflow; the Why This Plan recommendation and two alternative surfaces are selectable gear-choice cards that apply the chosen evidence-backed candidate to the highlighted chair, use only canonical five-piece set projections for those two-set packages, and expose confidence as a color-coded evidence state; the right-side gear picker can also select up to two individual five-piece sets across recommendation/alternative evidence, preserving that manual chair package into Raid Plan independently of any one observed two-set pairing; its Plan Name control is an editable saved-Raid-Plan picker, so selecting an existing plan loads it into Comp Maker while typing a new name creates a new Raid Plan on Save. Navigation into Comp Maker is plain navigation and never silently binds the page based on where the user came from. Generated Comp drafts remain composition evidence only and do not create or rename reusable Roster Team identities. The visible Comp Maker actions use **Load Players** for player/chair intake and **Auto-Fill Builds** for filling unresolved build/gear recommendations while preserving existing choices. The manual gear picker owns ordinary five-piece overrides only; Raid Plan handoff filters restored planned gear accordingly, and saving a manual five-piece choice preserves candidate monster/mythic/arena evidence instead of replacing the entire package. Phase 14 rebuild work now has a canonical typed **CompPlanState / CompChairState** boundary that round-trips through Raid Plan, preserves unrelated plan metadata/responsibilities, supports incomplete Recruit chairs, and persists per-chair Comp lock fields so user-fixed player/class/build/gear decisions survive save/reload. Raid Plan-bound Comp sessions now save this canonical state directly back to the originating Raid Plan without silently applying visible recommendations or routing through generated-roster draft persistence; the PLAN NAME picker and Raid Plan handoff both enter the same canonical state path. Comp Maker now participates in the app-wide unsaved-changes contract from canonical state: chair/context edits mark the plan dirty, Save persists canonical state, and Discard reloads the bound Raid Plan. Team Health is derived directly from canonical `CompPlanState`, including persisted planned gear and planned assignment intent, and separates open Recruit seats from unresolved gear gaps instead of treating every Recruit as ungeared.

## Phase 14 Builds progression ownership
- The Builds dossier now owns character progression access through a dedicated **Progression** tab.
- **Passive Skills** covers character-owned skill-line access and purchased passive ranks shared by every build for that character.
- **Passive Champion Points** covers non-slottable Champion stars shared by every build; slotted Champion Points remain build-specific on the normal CP tab.
- Character Progression provides global **Buy All Passive Skills** and **Buy All Passive CP** actions, while retaining per-skill-line and per-discipline controls for narrower edits.
- The old monolithic Build Editor, Character Progression workspace tab, and Scribed Skills workspace tab are hidden from the normal Phase 14 workflow and retained only as compatibility/fallback infrastructure.


## Rumors collection ledger
- Imported UESP Rumors stored in `collectible_rumor` / `collectible_rumor_hint` now populate the **Rumors** Collectibles page instead of showing an empty ledger.
- Rumor ownership is profile-aware through a separate additive `collectible_rumor_progress` table; the canonical collectible catalog is not rewritten or merged.
- Rumor detail shows the imported start hint plus ordered rumor-hint text, and Rumors progress is reflected on the Collectibles dashboard.


## Team Discord links
- Teams visibly expose an optional Discord invite/server/channel URL alongside raid schedule, time zone, and current focus on the current Phase 14 Teams editor.
- The Discord URL is persisted additively on the existing `team` record and reloads with the selected team.
\n\n## Raid Plan\- Raid Plan Coverage reconciles explicit buff/debuff ownership before generic provider availability: assigned providers are checked against saved-build and reviewed planned-set evidence, duplicate primary ownership is surfaced, and unassigned gaps remain visible without automatically moving support to a Tank or Healer.\nn\n- Saving a Raid Plan promotes any typed unknown gamertag into a minimal Personnel player record before stable identity resolution; it does not invent a character or build.\n- Team chairs use a neutral canonical skeleton: Tank 1/2, Healer 1/2, and DD 1–8. Empty chairs may persist class/build planning before a player is known; assigning a player fills the chair without changing its internal seat identity. Detailed raid duties such as Main Tank, Off Tank, kite, portal, tombs, add tank, and healer jobs remain separate Assignment-layer state rather than being encoded into the chair name.\n- The Team Class column uses the shared editable, case-insensitive contains autocomplete pattern over canonical ESO classes.\n- Trial-specific artwork banners appear in Raid Plan Selected Plan and Live Raid Current Encounter hero surfaces.\n\n\n## UI\n\n- Shared app page headers use the Collectibles-style uppercase Montserrat treatment with muted-gold text and restrained letter spacing; page-specific subtitles and context controls remain unchanged.

- **Rotation command-center visual contract** — Rotation Context stays on one compact row, intent tiles use restrained blue/silver hover/selected treatment, and generated results use the five-button icon navigator with a locked pre-generation state instead of exposed raw Qt tabs.

- **Raid Plan note** — the parchment Plan Note on Raid Plan Overview is editable and persists with the exact `RaidPlan` snapshot; loading another plan restores that plan's own note.
\n- **Rotation Builder Phase 14 shell** — Rotations are restored through an owned command-center page that preserves the current presentation while calling the existing generation and sustain engines directly; the legacy patched dashboard is not constructed at startup.\n
- Phase 14 Comp candidate provenance distinguishes **saved Builds** from **reference templates**: only real saved-build candidates may populate `selected_build_name`; reference templates keep source/candidate provenance and may contribute planned gear/class/Mundus without masquerading as an owned Build.
- Phase 14 saved Comp candidates carry stable canonical player, character, and build identity through the `PlayerBuild` compatibility snapshot. Duplicate-player prevention, provider-evidence recovery, saved-build binding, and provider-workload recovery use those stable IDs rather than treating display/source names as ownership.
- Phase 14 selected-chair Apply, Auto-Fill, and Team Health are canonical-state-only. `_comp_applied_candidates` remains a compatibility/presentation cache, while saved-player duplicate detection and applied-candidate recovery prefer `CompPlanState`.

- **OBS Field Note source overrides** — The Foundry dashboard Lua script exposes persistent OBS-side edit fields for `NOTE_Observation` and `FN_Location`. Blank values preserve the existing automatic JSON-driven text; entered values override the one-second overlay refresh safely.

- **Phase 14 planning workflow round-trip guard** — regression coverage now exercises the real cross-service path from Personnel → unbound Comp Maker plan → canonical Comp Build → persisted/reloaded Raid Plan → stable BuildId resolution → Coverage assignment semantics → Readiness. Brand-new plans repair Comp Build source-plan provenance immediately after first Raid Plan binding without duplicating the build.


### Personnel lifecycle and history
- Personnel records support **Archived** status.
- Archived players are excluded from normal roster queries and current planning by default.
- Archived players remain available through the explicit **Archived** view and can be restored.
- Permanent deletion requires the Personnel record to be archived first.
- Personnel records retain private freeform notes.
- Former gamertags are stored as durable exact player aliases and remain available on archived records.
- Exact alias matching excludes archived players from active identity resolution while preserving their historical aliases.



### Team deletion and cleanup
- Teams can be deleted directly from the Teams overview or Team Schedule.
- Team deletion removes the team record, schedule, roster memberships, and canonical build assignments for that team.
- Deleting a team never deletes Personnel, characters, saved builds, or historical raid-plan snapshots.
- Use Team Merge instead of Delete when a duplicate team contains roster/build-assignment data that should be preserved.



### Live Raid encounter context
- Live Raid includes an **Encounter** selector filtered to the selected Raid Plan's trial, plus **Trial / General**.
- The selected encounter is stored as per-plan manual run state and survives pull start/end actions without mutating the Raid Plan.
- Selected encounter context is projected from canonical boss-guide and reviewed encounter-evidence services.
- The hero card shows the selected encounter and reviewed phase guide without claiming unobserved live boss phase.
- **Next 60 Seconds** projects only reviewed wall-clock encounter facts from canonical persistence, recalculates the visible window from the manual pull timer, and keeps health/phase thresholds labeled as guide context rather than converting them into invented clock times.
- **Boss-aware attempt history** persists every manual pull independently of notes, captures the selected encounter at **Start Pull**, records end time and duration at **End Attempt**, shows recent attempts in Live Raid, supports encounter-filtered history, and carries encounter identity into the one-note-per-attempt Review record.
- **Raid Review attempt journal** is attempt-first rather than note-first: every recorded pull appears even when no note was saved, grouped by date → trial → encounter, with canonical encounter names when available, duration/timing metadata, and optional attached run notes.
- **Raid Review encounter summary** aggregates manual attempt history by trial + encounter with total pulls, completed/timed pulls, average duration, best duration, and attached-note count. Untimed/active pulls count as pulls but never contaminate timing statistics; no success/kill outcome is inferred.
- **Current Callouts** combines reviewed encounter handling with encounter-scoped Raid Plan responsibilities.
- **Next 60 Seconds** uses only reviewed facts with explicit persisted wall-clock fields; health/phase thresholds remain threshold-based and are never converted to guessed timestamps.
- **Quick Notes / Run Sheet** shows read-only encounter checklist context separately from editable manual run notes.
- **Recent Events** remains manual run-state history.
- **Coverage Snapshot** remains owned by the existing Coverage system rather than duplicating coverage logic inside Live Raid.



### Live Raid linked Raid Maps
- The Live Raid Raid Spots footer uses two equal actions: **Assignments** and **Raid Map**.
- Raid Map links are stored as user-owned operational state keyed by **Raid Plan + Encounter**.
- The map image remains owned by the existing encounter Raid Map store; Live Raid persists only the chosen map id.
- A Live Plan can use a different saved map for the same encounter than another plan.
- Live Raid can link/change/clear the chosen map and open the exact linked encounter/map in Mechanics.



- **Finch sub-ready build handoff** — shared Raid Plan publishing now exposes only the selected seat's operational build subset needed for Discord execution handoff: planned gear, front/back weapon labels, front/back skill bars, Mundus, food, potion, and Raid Plan assignments. Private notes, local IDs, source URLs, and full build payloads remain local.

- **Finch Discord persona binding** — Roster Sync binds Finch registrations to Personnel by stable Discord user ID, updates the public Discord name to the member's current display name, and records prior Discord names/gamertags in private Known Aliases history with first/last-seen provenance.

- **Encounter map plan attachment** — Encounters Raid Map has an optional Raid Plan selector (None by default) persisted with the tactical layout. Attach to… captures the current board directly into the selected Mechanics & Timelines encounter; when a Raid Plan is selected, the same action also creates the Live Raid plan+encounter map link.

- **Live Raid map visibility** — When the current Raid Plan + encounter has a valid linked Raid Map, Live Raid shows `Map Available` beneath the phase guide and lists `MAP  <Encounter> • <Map Label>` in Recent Activity. Link/change/clear updates both surfaces immediately.

- **Raid Plan Team selector** — Raid Plan now exposes an optional Team dropdown between Plan and Saved Plan. It defaults to No Team, persists `team_name` on Save, restores the saved team on Load, and uses the selected team for Personnel/team membership synchronization.

- Raid Map plan assets: Raid Map can explicitly save its current tactical layout to the selected saved Raid Plan; Raid Plans can hand off directly to the plan-scoped Raid Map editor; Live Raid continues to consume encounter-linked Raid Maps; shared Raid Plan schema v6 publishes the plan's Raid Map link manifest for Finch/website consumers without embedding private local paths.


- **Raid Map WebP publishing to Finch** — Encounters → Raid Map now exposes **Save Map to Raid Plan** for the rich editable local layout and **Save Map to Finch (WebP)** for a compact raid-night snapshot. The Finch action captures the current board, renders a flattened WebP, uploads it to Finch, records the public preview metadata, and republishes the saved Raid Plan with only the minimal preview fields needed by mobile/web clients.

- **Raid Map seat-to-player projection** — Encounters keeps stable chair labels such as Tank1, Healer2, and DD4 in the editable map. Saving a Raid Plan map creates a player-name projection from that exact layout for Live Raid, and the same player-labelled image is the source Finch converts to WebP. Live Raid shows the linked map inline beside Raid Spots, while reusable Boss Templates remain seat-labelled and plan-independent.


- **Live Raid Raid Spots tabs** — Live Raid keeps the full Names & Assignments roster table on the first Raid Spots tab and gives the linked Raid Plan map its own full-width Raid Map tab, so spatial assignments such as house stacks are readable without squeezing the roster or leaving the page.


- **Live Raid Boss Mechanics loop** — Live Raid includes a context-aware Boss Mechanics shortcut that opens Mechanics & Timelines directly on the currently selected encounter. Mechanics shows a temporary **← Back to Live Raid** control only for that handoff, returning to the same Live Raid plan/encounter context without making the user reselect raid state.

### Objective #32 theoretical closure hardening
- Candidate ranking now treats shared unresolved potion timing/restoration/cooldown evidence, Heavy Attack completion/hit/restoration evidence, and canonical character-progression ownership gaps as hard failures when they can change the measured resource or damage timeline.
- Shared display/metadata-only warnings remain advisory, so fail-closed mechanics evidence does not turn unrelated labels into false blockers.



- **Live Raid full-map popup** — The Raid Map tab keeps the normal fitted view and adds **Open Full Map**, which opens the same linked Raid Plan map in a large modal with Fit / zoom out / zoom in controls. Closing the modal returns immediately to Live Raid.


### Objective #32 weapon standing passive runtime coverage
- Phase 5 static contexts now apply reviewed U50 max-rank weapon standing branches using current-live rank values: **Twin Blade and Blunt** swords (+129 Weapon/Spell Damage each), daggers (+657 Critical Chance rating each), maces (+1487 Offensive Penetration each), and **Heavy Weapons** greatsword (+258 Weapon/Spell Damage) / maul (+2974 Offensive Penetration). Axe Critical Damage branches remain fail-closed pending refreshed current-version proof.
- These effects require explicit character passive ownership/rank evidence in canonical progression and exact active-bar weapon subtype evidence.
- **Ambidextrous** now resolves its reviewed max-rank +6% of verified CP160 Gold off-hand weapon damage into flat Weapon/Spell Damage; missing off-hand item-power evidence fails closed.
- **Bow Accuracy** now resolves its reviewed Rank 2 +1314 Critical Chance rating only while the Bow is on the active bar, using the shared canonical critical-rating conversion.
- The weapon-passive proof audit rejects Rank 1 values presented as max-rank evidence, preventing 64 / 129 / 3% from being accepted for Twin Blade swords / Heavy Weapons swords / Ambidextrous when Rank 2 ownership is required.
- Generated sustained-DPS exact evaluation inherits this Phase 5 fail-closed behavior before simulation, so unresolved weapon-passive branches cannot produce a closure-ready theoretical candidate.


### Objective #32 passive rank evidence authority
- Rank-specific passive mechanics now have a shared fail-closed description authority that joins skill_rank.ability_id to the ESO game identity ability.ability_id, not the ability table local row ID.
- When skill_rank.raw_description and the exact-rank ability.description both exist but disagree after normalization, the passive rank is unresolved instead of silently preferring one source.
- Extreme's global player-skill universe carries max-rank passive source disagreement as explicit unresolved evidence, and ExtremePassiveProjectionService refuses to turn an otherwise parseable tooltip into static objective math while that disagreement exists.
- Deadly Bash and the weapon-passive challenger audit now consume this rank evidence contract.


### Objective #32 shared sustain evidence hard gates
- Shared unresolved canonical action-cost evidence can no longer remain ranking-eligible merely because baseline and candidate inherit the same missing cost.
- Sustain tags unresolved racial/armor/jewelry cost-modifier evidence with explicit action-cost-modifier provenance, and shared tagged modifier uncertainty is a hard ranking failure.
- Cosmetic cost/display metadata remains advisory; the hard gate is limited to evidence that changes actual resource spend.


- **FoundryDock user database split** — Replaceable ESO/reference facts remain in `data/eso.db`, while Personnel, Teams, roster workflow state, collectible ownership/profiles, and achievement progress live in writable `foundrydock.db`. Startup performs additive legacy migration without deleting or replacing the source. Source runs use `user_data/foundrydock.db`; frozen builds use `%LOCALAPPDATA%\FoundryDock\foundrydock.db`. Friend and release packaging can optionally embed an explicit first-run `foundrydock.db` seed for deliberately customized EXEs, and existing user databases are never overwritten.


- **Privacy-limited current raid EXE seed** — Saved Raid Plans now persist in `foundrydock.db` alongside Personnel/Teams rather than `data/raid_plans.json`. Legacy Raid Plan JSON is copied additively on startup. The friend-build switch `-UseCurrentRaidSetup` creates a one-off first-install seed containing only Personnel, Teams, memberships, Personnel assignments, and one selected saved Raid Plan; achievement and collectible progress are deliberately excluded. If multiple plans exist, `-RaidPlanId` is required rather than silently packaging unrelated plans. The same custom profile filters `characters.json` down to only player/character/build records referenced by that selected plan.


- **FoundryDock crash log** — Uncaught main-thread and worker-thread exceptions append to `FoundryDock-crash.log`. Frozen builds write the log beside `FoundryDock.exe`; source runs write it at the project root. Each entry includes a UTC timestamp and full traceback so packaged crashes can be diagnosed without relying on the Windows exception dialog.


- **Build and character database migration** — Canonical player, character, progression, saved-build, Comp Build, and build-to-team assignment state now persists in `foundrydock.db`. Legacy `data/characters.json` and `data/builds.json` are migration inputs only and are no longer created in new first-install packages. Application-facing build catalog paths redirect to the user database so downstream build/rotation/optimizer consumers share the same authority.


- **Split weapon-enchantment cooldown-topology authority** — Objective #32 now tracks duplicate-identity shared cooldowns and distinct-identity independent cooldowns as separate proof requirements. Exact finite enchant sequencing remains fail-closed until both topology claims are authoritative, preventing a generic `per_effect_identity` label from silently proving more than the evidence actually supports.


- **Raw weapon-enchantment metadata audit** — A read-only research tool inventories every field in the raw UESP `minedItemSummary` weapon-enchantment export and highlights cooldown-, ability-, enchant-, proc-, duration-, effect-, and ID-related metadata with sample values. It never opens or mutates `eso.db`, giving Objective #32 a safe way to discover importable cadence metadata before any additive schema decision.


- **Finch Discord identity reconciliation** — shared Team schema v3 includes the roster member's explicitly stored Discord username so Finch can exact-match the interacting Discord account to canonical Personnel without fuzzy-name guessing. Finch's immutable Discord user ID remains its stable private binding once observed.
- **Finch seat confirmation intake** — Finch raid confirmations can be synchronized into Raid Readiness as explicit FINCH human-ready evidence keyed by Raid Plan and seat; this updates user-owned readiness state without replacing ESO/reference data or the FoundryDock database.


- **Selected weapon-enchantment damage-policy bridge** — Exact source-selected glyph proc consequences can now project reviewed damage-policy semantics without pretending to own final damage math. Oblivion weapon-enchantment damage resolves as non-critical from primary ZOS evidence; ordinary elemental/absorb glyph critical eligibility remains explicitly unresolved rather than inheriting normal skill rules. Raw canonical magnitude is preserved for later target-side application.
- **Canonical Oblivion damage-policy gate** — Exact CP160 Legendary Decrease Health application now passes through the shared weapon-enchantment damage-policy proof before entering the sequential target-Health ledger, so the reviewed non-critical Oblivion exception has one canonical authority. Ordinary elemental and absorb glyph damage still fail closed until their critical rules are proven.

- **Caller-owned poison consequence authority seam** — The canonical Objective #32 runtime-state factory can accept either a complete weapon-poison consequence resolver or explicit per-poison dilution-selection authority. Explicit dilution proof is wrapped through the canonical named-effect consequence frontier, while competing authorities are rejected and missing proof remains fail-closed. Closure-ready Objective #32 composition/preflight now require poison activation and consequence authority paths instead of accepting a poison-blind runtime graph.
- **Generated front/back weapon-poison selection axis** — Objective #32 physically enumerates the canonical crafted-poison formula catalog independently on the front and back weapon bars, including no-poison choices and one-bar legality. The selected formula identity survives candidate assembly as bar-owned build state before runtime-state derivation, and `weapon_poisons` is a first-class canonical mutation axis. The independently searched source-backed solvent/level denominator is separately represented by `weapon_poison_tiers`; formula coverage can no longer stand in for tier coverage. Formula selection proves the trait set only; remaining dilution/runtime consequence semantics stay separately evidenced.

- **Finch stale-identity repair** — Sync Finch now checks the current little Discord username against Personnel before trusting an older Discord-ID binding. A unique exact username match can repair a previously misbound account, rebind the Discord ID locally, and push the corrected canonical player name back to Finch. Fuzzy matching remains disabled.


- **Fail-closed weapon-enchantment cooldown-rule validation** — Objective #32 now treats malformed equipped-weapon cooldown-rule results as unresolved evidence instead of allowing missing/non-numeric cooldown or reduction metadata to crash exact runtime policy projection.
- **Candidate build context for callable enchant cooldown policies** — runtime scenario composition now passes the finalized `PlayerBuild` through both object-based and callable weapon-enchantment cooldown-policy seams, so production/test resolvers can apply exact equipped trait/quality provenance consistently.

- **Infused weapon-enchantment cooldown rule split** — weapon-enchantment resolution now treats Infused magnitude and cooldown reduction as separate canonical trait consequences. Saved `Gold` quality normalizes to database `Legendary` for the quality-dependent magnitude, while the independent canonical cooldown-reduction rule is applied to runtime cooldown math instead of being lost behind the magnitude row.

- **Canonical Objective #32 runtime-state factory** — production sustained-DPS wiring can now build candidate-scoped runtime state from one shared ESO repository/rule graph, including dedicated weapon-enchantment source/variant projection, activation-event classification, candidate scaling, and fail-closed cooldown policy. Pre-runtime exact damage occurrences remain an explicit injected authority so runtime state is not circularly derived from the final evaluator that consumes it.

- **Selected weapon-enchantment consequence coverage gate** — exact generated sustained-DPS leaves now recover the canonical consequences of each source-bound glyph proc and refuse mechanical completeness when any selected damage, restoration, buff/debuff, or other consequence lacks an explicit runtime consumer. Source selection and cooldown sequencing can no longer make an unscored glyph consequence silently disappear.

- **Weapon-enchantment trait-adjusted scaling provenance** — trait-adjusted weapon-enchantment effects now preserve canonical `scaling_type` and condition metadata instead of reconstructing a stripped CombatEffect. This prevents Infused or other trait processing from erasing target-health scaling and other runtime semantics before exact scoring.

- **Selected Crusher exact target-resistance projection** — source-bound Crushing glyph procs now become exact-duration resistance-reduction windows and feed the same canonical target-resistance path used by other reviewed reductions. Missing duration/target/magnitude or overlapping selected windows remain fail-closed rather than inventing stacking behavior.

- **Selected Weapon/Spell Damage glyph runtime projection** — exact selected Weapon/Spell Damage glyph procs now become active timed `weapon_spell_damage` variants and reuse the existing runtime stat bridge, so their proven windows rebuild Weapon/Spell Damage in the ordinary exact-time build context instead of living only as source-selection evidence.


- **Exact Decrease Health runtime damage integration** — selected CP160 Legendary Decrease Health glyph procs now enter the generated sustained-DPS target-Health ledger as exact Oblivion outgoing damage, so they can alter later execute-state math, terminate a fight, and appear in canonical damage summaries instead of living in an isolated policy service.
- **Supplemental deterministic damage ledger** — canonical DD Combat Simulation can now interleave exact supplemental outgoing-damage events with action and periodic damage while preserving live target-Health feedback; unresolved same-instant cross-source ordering fails closed.


- **Bar-specific poison ownership** — saved builds, legacy boss alternates, generalized Team/Boss context variants, canonical Bar state, and the Builds editor now preserve front- and back-weapon-set poisons independently rather than collapsing poison state into one character-wide flag.
- **Authoritative poison/enchantment suppression** — weapon-enchantment runtime source projection now omits glyph sources only on the exact weapon set carrying an equipped poison, preserving the other bar. This uses current ESO Support evidence that poisons suppress enchantments on the weapon set where the poison is equipped; poison proc cadence and effects remain separately fail-closed.
- **Canonical poison picker source** — ReferenceDataService now exposes canonical entity_type='poison' names for build editors without altering or rebuilding data/eso.db.


- **Authoritative weapon-poison cadence evidence** — Objective #32 now tracks poison proc chance, 10-second global cooldown scope, qualifying Light/Heavy/weapon-ability damage triggers, single-target DoT exclusion, and enchantment suppression as separate ZOS-evidenced fields. The cadence evidence is runtime-ready without borrowing weapon-enchantment assumptions.

- **Shared Performance Mode U50 build templates** — Builds → Templates ships a reusable Performance Mode U50 package for the current Sorcerer/Necromancer tanks, Warden/Arcanist healers, and Necromancer/Nightblade/Arcanist/Dragonknight/Templar DD roles. Templates are player-neutral, so Jarakeen, Rylo, or another Personnel character can apply the same class-compatible package without copying player identity, progression, team membership, or replacing the user database. The bundled records preserve current bars, planned gear packages, DD 1-Light/6-Medium guidance, Perfected Merciless Charge back-bar ownership, and human-readable Class Mastery selections for review.

- **Personnel duplicate merge + alias-safe roster import** — Personnel exposes an explicit **Merge Duplicate Player…** action: select the canonical player to keep, choose the duplicate to absorb, and FoundryDock preserves teams, assignments, canonical characters/builds, and the discarded name as alias history. Roster import now resolves exact learned aliases before creating Personnel, so a name previously merged as an alias updates the canonical player and imports builds under that canonical gamertag instead of recreating the duplicate. Ambiguous aliases fail closed and require an explicit merge.


- **Finite weapon-poison consequence runtime frontier** — Objective #32 can now bind each selected poison proc branch to explicit source-bound runtime consequence attempts and carry those effects into the ordinary runtime-state frontier. Poison chance and the player-global cooldown remain owned upstream, so consequence effects cannot reroll the 20% proc decision. A configured resolver must return an actual finite attempt frontier and explicit runtime effects; resolver presence alone no longer clears the poison blocker.


- **Crafted weapon-poison item evidence frontier** — Extreme Engine can now resolve a saved crafted poison item label against imported UESP Alchemy Poison tier evidence and recover the source-backed universe of effects that may occur under that label, together with each effect page's base/triple duration alternatives. The label alone does not prove the exact ingredient formula, exact effect set, or dilution duration, so selected consequences remain fail-closed until formula/effect provenance is supplied.


### Raid Plan save hardening and overview consolidation
- Raid Plan overwrites create a recoverable whole-database safety snapshot before the transactional write.
- Saves fail closed on stale persisted snapshots, duplicate occupied players, read-back mismatches, and suspicious bulk player-identity replacement across six or more seats.
- Recovery drafts remain separate from durable saved state until an explicit Save.
- The Raid Plan overview uses one plan-action card; duplicate Quick Actions and Linked Resources button cards are removed.
- Team Schedule reads the existing Team-owned schedule from RosterService, including recurring time/timezone and current focus, instead of creating a second schedule authority.


- **Objective #32 poison provenance ladder and first exact target consequences** — Weapon-poison modeling now keeps item-label possibility evidence, explicit canonical formula/effect-set proof, and explicit base/triple dilution proof as separate authorities. With those witnesses supplied, reviewed U50 Breach poison consequences project Minor Breach through the shared target-resistance path and Protection poison consequences project Minor Vulnerability through the shared Damage Taken path; Protection's self Minor Protection side is explicitly collapsed as defensive-only for sustained outgoing DPS. Saved bottle labels alone still cannot satisfy formula/dilution provenance, and unreviewed traits such as Ravage Health remain fail-closed.


- **Exact-time SELF named runtime projection** — Generated sustained-DPS evaluation now projects reviewed SELF-target named runtime effects into the attacker CombatState at each exact action timestamp. This gives source-bound effects such as poison-granted Minor Brutality/Sorcery/Savagery/Prophecy a canonical path into attacker stats without duplicating named-buff arithmetic; ENEMY-target effects remain owned by the separate target combat-state projector.

- **Comp Maker legacy-chair identity repair** — Saved Raid Plans whose older/typed chairs have a player name but no stable Personnel ids now resolve that name through the canonical exact-name/learned-alias identity service when loaded into Comp Maker. A unique Personnel match carries its roster/player/character ids into the chair so Comp Builds and later Raid Plan saves retain the real player identity; ambiguous or missing matches remain unresolved rather than guessing or borrowing another chair's identity.


### Comp Maker saved-Build source separation
- Comp-generated `BuildKind=comp` chair packages remain Raid Plan planning/recovery evidence, but are no longer fed back into Comp Maker as reusable saved-Build candidates.
- User-created canonical Builds remain eligible by role/class/player identity after restart, preventing a plan's own `Planned` package from hiding the player's reusable Build.


### Saved Build / Template / Raid Plan assignment boundary
- Reusable user Builds remain canonical Saved Builds owned by a Character; Build Templates remain reusable reference material.
- Comp Maker no longer creates new `build_kind=comp` records when a Raid Plan is saved.
- Raid Plan chairs now persist a Saved Build reference when one is explicitly selected, plus Raid Plan-owned planned gear/skills/Mundus as overrides.
- A chair with planned configuration but no reusable Saved Build is represented as Planned Raid Plan state, not as another Build.
- Legacy `build_kind=comp` records remain readable for revision/recovery compatibility and are detached from active chair Build assignment when encountered; they are not deleted automatically.

- **Alchemy formula source-section provenance** — The canonical Alchemy formula catalog now preserves reviewed UESP formula-section provenance when duplicate formula evidence is merged. Poison formula selection exposes those sections as evidence without treating a heading as automatic base/triple dilution proof, keeping the remaining duration witness fail-closed instead of guessing from recipe layout.

- **Generated formula vs saved poison identity guard** — Saved crafted-poison item evidence now rejects generated `alchemy_formula:...` identities explicitly instead of querying them as if they were inventory labels. Generated formula provenance and saved Poison IX item-label evidence remain separate authorities, preventing formula selection from silently masquerading as poison-tier/duration proof.

- **Generated poison-loadout provenance retention** — Final assembled Objective #32 candidates now retain the selected front/back poison loadout object alongside the flattened build fields. Downstream runtime and consequence authorities can inspect exact generated formula provenance directly instead of reconstructing it from `FrontBarPoison` / `BackBarPoison` strings.

- **Candidate-scoped poison consequence authority** — Objective #32 runtime-state resolution can now resolve a poison consequence authority from each complete generated candidate state instead of requiring one global resolver for every formula choice. The factory enforces one authority path at a time, allowing future generated formula/tier/dilution proof to remain candidate-specific without weakening the existing fail-closed boundary.

- **Generated weapon-poison tier search** — Exact generated Alchemy formulas now enumerate every imported Poison solvent/level coordinate shared by all formula traits, then search front/back tier choices independently. The selected tier loadout is retained on the assembled candidate and consumed by runtime according to the proc source bar; no crafted item label or preferred highest tier is invented.

- **Per-effect weapon-poison dilution witnesses** — Exact poison-duration selection now supports effect-by-effect ordinary/triple modes instead of forcing one bottle-wide switch. Canonical Alchemy formulas retain literal per-trait `(triple)` source annotations separately from unmarked cells; missing, conflicting, or extra effect-mode proof remains fail-closed.
- **Extreme sustained-DPS poison formula dilution witness** — promotes literal per-trait `(triple)` annotations preserved on canonical Alchemy formulas into exact per-effect triple-duration witnesses. Unmarked cells, implicit page effects, formula section headings, and effect count remain provenance only and never become guessed base-duration mechanics.

- **Roles return and Saved Build Coverage** — Returning to Roles refreshes Personnel and Builds before restoring the full saved chair selection, preserving gamertags instead of showing the first Personnel entry. Raid Plan Coverage now reads front and back bar skills from the exact assigned Saved Build, combines them with planned skills, and identifies reviewed skill effects and matching class skill-line passives as conditional group coverage.
- **Finch destination labels** — Finch navigation says “To Finch”; Team, Raid Plan, and Raid Map publishing to FoundryDock.my says “To FD.my”; shared Coverage and Readiness publishing says “To Bff”; fetching shared work says “Check Mail.”

- **Coverage from assigned healer skills and equipped sets** — Combat Prayer on an assigned Saved Build supplies Minor Berserk as covered group evidence. An equipped two-piece Ozezan the Inferno supplies Minor Vitality. Both show their named source and count as Covered in the Raid Plan summary; their activation and uptime remain conditional, and a single monster piece does not count.
- **Candidate-scoped weapon-enchantment cooldown proof** — exact runtime cadence now requires only the topology facts that can affect the concrete enchantment universe. A single enchant identity can use authoritative cadence without being blocked by unanswered different-identity behavior; duplicate copies require the authoritative shared-timer rule; mixed identities still fail closed until cooldown scope and distinct-identity independence are proven.
- **Poison-tier-stable generated candidate coordinates** — generated sustained-DPS candidate identity now includes both the weapon-poison formula loadout index and the independently searched poison-tier loadout index. Absent poison axes use `-1` rather than colliding with valid frontier index `0`, and different solvent/level tier branches can no longer collapse onto the same refinement coordinate merely because they share the same formula.
- **Canonical poison-tier mutation axis** — the independently searched front/back crafted-poison tier denominator is now a first-class Objective #32 mutation axis rather than an unregistered tree tag. Theoretical closure must therefore prove both weapon-poison formula coverage and poison-tier coverage, and generated-axis inventory now rejects malformed non-canonical axis tags instead of silently ignoring them.
- **Objective #32 poison-tier composition gate** — the canonical production composition root now refuses closure-ready construction unless the generated late-axis adapter supplies both the weapon-poison formula frontier and the independent weapon-poison tier frontier, and physically publishes both canonical axis tags from its `axes()` surface. This prevents a graph from advertising complete poison search while physically enumerating only formula identities.
- **Poison-tier malformed-source closure gate** — source-backed poison tier enumeration now fails closed when a wanted formula trait has malformed or non-object imported Poison payload data. Unreadable source rows can no longer be silently discarded while claiming the solvent/level denominator is complete.
- **Assembly-authoritative generated poison provenance** — candidate-scoped poison runtime authority now trusts formula and tier provenance only from the assembled generated candidate. Stale pre-assembly late-axis state cannot backfill missing assembled witnesses, preventing mismatched formula/tier state from being revived after cross-axis assembly.
- **Schema-ready Extreme Engine contracts** — core generated candidate coordinates, poison tier/frontier records, poison item-evidence records, runtime-state frontier records, candidate runtime resolvers, global generated search inputs, Objective #32 proof/preflight boundaries, generated tree nodes/search results, optimistic bound envelopes, rotation upper bounds, pruning decisions/results, finite runtime-attempt evidence, external-history assembly, runtime event skeletons, runtime effect universes/relevance/scaling results, weapon-enchantment sequence histories, and weapon-poison activation/sequence histories now validate their own invariants instead of relying on truthiness or downstream failures. Index domains, identity relationships, candidate-count agreement, retained proof evidence, proof-state consistency, finite numeric ceilings/durations/probabilities/timestamps, strict proof booleans, normalized diagnostics, callable resolver contracts, typed blocker relationships, mutually exclusive trigger families, and canonical nested record types are enforced close to construction. Resolver dispatch now prefers explicit callable `resolve()`/`build()` methods before generic callability so class-based service authorities are not accidentally instantiated as plain functions. The rules are deliberately dataclass/native-Python today so they can migrate incrementally into Pydantic validators later without changing semantics.

- **Coverage PDF field sheet** — the Coverage workspace can export the selected saved Raid Plan's reconciled covered effects to a low-ink landscape PDF showing Effect, Provider, Backup, exact manual Raid Lead source/notes when present, static/planned evidence, and Coverage status. Export reads the same rendered Coverage table used on-screen, includes rows hidden by filters, excludes true gaps, and preserves current Raid Plan state without creating another persistence path.
- **Raid Plan Build sharing on Overview** — the Critical Damage & Penetration card offers a visible Copy Builds for Discord action. Beside the plan backup control, Export XLSX produces a styled, filterable workbook with linked Saved Build skill bars, planned skills, gear, consumables, and assignments; Export PDF produces a low-ink, printer-friendly Build sheet. Both identify unresolved chairs without guessing a Build.

- **FoundryDock Raid Build field-packet exports** — Raid Plan XLSX/PDF exports now use a low-ink FoundryDock treatment with a raid index and detailed per-player build sections. They include armor slot/body-part assignment, armor weight or weapon type, traits, enchants, front/back skill bars with slot ownership, and full scribed-skill formula components while intentionally omitting the assumed Gold / CP160 baseline.

- **Explained manual Coverage providers** — on Coverage, select an effect and use Add Provider to mark it covered for the selected saved Raid Plan. The raid lead must choose a plan seat and enter the skill, set, passive, or other source; an optional note records the reasoning. Manual assertions persist inside the Raid Plan, count as planned coverage, remain visibly labeled `Raid Lead Assigned` rather than Build-confirmed evidence, and can be removed without editing computed evidence.

- **Pydantic-hardened Raid Plan persistence** — persisted Raid Plan payloads now cross a strict Pydantic v2 validation boundary before domain construction and again before save. Manual Coverage requires bounded nonblank effect/seat/source fields, rejects unknown seats, duplicate effect/seat assertions, malformed collections, and unexpected schema fields, while legacy plans without `coverage_providers` continue to load with an empty collection. Frozen dataclasses remain the domain model; Pydantic is the persistence/import boundary rather than an invasive rewrite.

- Builds **Endgame baseline** now also supports optional whole-build defaults for Armor Trait, Armor Weight, Armor Enchant, Jewelry Trait, and Jewelry Enchant. Blank equipped slots inherit the selected defaults for calculation/readiness paths while explicit per-slot values remain authoritative; leaving a new dropdown blank preserves per-slot control.

- Builds baseline materialization now writes selected defaults into blank fields on equipped Saved Build gear when the baseline is saved, while preserving every explicit per-slot value. Build-profile JSON is validated through a strict Pydantic v2 boundary before persistence/readback; known armor/jewelry traits and armor weights are validated and unexpected profile fields are rejected.

- Raid Plan PDF/XLSX build field packets now use an ink-aware FoundryDock visual hierarchy: teal/gold accents, pale field-journal section bands, restrained alternating row shading, stronger title treatment, print margins/footers, and dedicated wrapped Assignment/Utility treatment in PDF rather than squeezing raid instructions into the summary grid.

- Manual Coverage persistence now preserves the selected Raid Plan across Coverage refreshes, verifies provider add/remove operations by immediate canonical repository read-back, and keeps provider payloads behind the strict Raid Plan Pydantic v2 boundary. Coverage effect/source text is normalized and rejects unsafe control characters; provider seat references and duplicate effect/seat assignments remain fail-closed.

- Builds now includes a class-aware **Class Masteries** workspace backed by the canonical ClassMasteryRepository. Pure-class Saved Builds can choose up to two passives from their own class, see canonical descriptions as tooltips, persist canonical ability IDs on PlayerBuild.ClassMasteryAbilityIds, and verify the values by immediate BuildService read-back. Subclassed builds fail closed because ESO Class Mastery is unavailable while subclassed.

- **Coverage persistence guard** — Raid Plan repository saves now preserve an existing non-empty manual Coverage provider overlay when a generic/stale screen submits the same plan with the default empty Coverage collection. Explicit Coverage edits still use optimistic expected-state saves, so intentionally removing the final provider remains possible. This prevents unrelated Raid Plan screens from silently erasing Coverage data.

- **Phase 14 Class Masteries editor** — Class Masteries now live directly in the current dossier-style Builds workspace as their own tab, using the canonical class-aware mastery catalog and saving up to two mastery IDs on the selected Saved Build. The normal workflow no longer routes Class Mastery editing through the superseded monolithic Build Editor.

- **Pydantic-hardened Phase 14 Build mastery/world state** — the current dossier Build editor validates class identity, Vampire/Werewolf exclusivity, subclass/Class-Mastery incompatibility, unique positive mastery IDs, the two-mastery limit, bounded unique class-skill-line identities, strict booleans, and unexpected fields before mutating Saved Build state. This boundary covers the recent Class Masteries and WW/Vamp editor additions without routing through the legacy monolithic Build Editor.

- **Class Mastery save crash hardening** — Class Mastery validation now distinguishes a pure-class route that explicitly records all three native class skill lines from a true subclass route containing foreign/replaced lines. Both Phase 14 mastery save surfaces validate before mutation, stop immediately when the canonical Build save fails, restore the prior mastery selection on failure, and contain read-back exceptions instead of letting them escape into the Qt event loop. The shared Builds save path now returns an explicit verified success/failure result so callers cannot continue after a failed persistence operation.

- **Extreme sustained-DPS proof-boundary hardening** — runtime attempt and external-history builders now require strict boolean denominator proof flags and canonical nested records before finite-family assembly. Generated branch-and-bound results reject duplicate evaluated/best candidate identities, require key-based membership, and permit unique-leader proof only when the proven global maximum has exactly one best candidate. This prevents truthy strings, malformed nested payloads, or duplicate identities from being laundered into Objective #32 proof state.

- **Extreme theoretical-closure and axis-proof contract hardening** — Objective #32 theoretical closure now requires a strict boolean mechanics-closure claim plus tuple-shaped blocker inventories, and canonical axis coverage refuses string/list denominators or duck-typed proof records. Truthy strings and iterable-shaped impostors can no longer be converted into complete theoretical or mutation-axis proof.

- **Extreme same-tree proof promotion hardening** — canonical generated-tree coverage now requires an actual boolean finite-maximum proof before physically searched mutation axes can be promoted into Objective #32 coverage. Truthy strings can no longer turn an unresolved generated tree into same-denominator coverage evidence.

- **Extreme pruning-proof hardening** — action-dominance proofs now require stable candidate identity, tuple-shaped dominated/required/unresolved collections, non-boolean finite numeric ceilings, and canonical occurrence/dominance records before exact action damage can become a pruning-safe optimistic bound. This closes Python bool-as-number and string-as-axis proof loopholes at the branch-and-bound boundary.

- **Extreme branch-pruning evidence hardening** — pruning now accepts only tuple-shaped canonical bound evidence, while bound/decision/result diagnostics require immutable tuple collections before branch disposition is calculated. Duck-typed or mutable evidence can no longer reach the prune/survive/forced-open decision boundary.

- **Extreme structural-ceiling proof hardening** — maximum damage-action counts now require true non-boolean integers, structural safety flags are strict booleans, absolute action ceilings reject boolean-as-damage values, proof sources/collections are validated, and count × damage branch ceilings accept only canonical proof records. Structural pruning can no longer gain authority from coercible proof-shaped inputs.

- **Extreme generated-axis proof-gate hardening** — Objective #32 generated gear, late-build, runtime-policy, runtime-state, rotation, and pipeline adapters now fail closed on non-boolean proof/completion flags, boolean-as-number inputs, mutable proof collections, and duck-typed runtime frontier evidence; regression coverage protects these search-tree promotion boundaries.

- **Extreme finalized-evidence admission hardening** — Finalized potion timing and generated leaf evaluation now require canonical proof evidence, strict boolean completion/denominator flags, immutable evidence collections, canonical generated candidates/frontier nodes, and non-boolean numeric runtime inputs before exact Objective #32 evidence is admitted.

- **Extreme search-result and blocker-boundary hardening** — Objective #32 branch-and-bound now requires canonical roots, child branches, exact-leaf evidence, and immutable proof collections; blocker diagnostics likewise refuse mutable proof-state collections instead of silently normalizing them before theoretical-closure reporting.

- **Extreme runtime-frontier proof-record hardening** — Objective #32 runtime-state families, scenario preflight, and candidate-scoped runtime resolution now require strict boolean closure flags, immutable canonical proof collections, canonical runtime choices/scenario results, and fail closed on truthy strings or mutable resolver outputs.

- **Extreme runtime-scenario and Ultimate-legality proof hardening** — Objective #32 runtime scenario results now require canonical runtime-frontier evidence and immutable proof collections; scenario denominator inputs and poison-consequence denominator evidence fail closed on non-boolean values; exact Ultimate added-action proofs likewise refuse truthy non-boolean legality flags and mutable legality diagnostics.

- **Raid Plan Crit/Pen cap indicators** — Raid Plan → Overview now explains personal, raid, and effective offensive values in plain language; combines Physical/Spell personal Pen into a compact P/S column; and shows per-player Critical Damage and Penetration status with exact shortage/excess. The calculator preserves uncapped raid Critical Damage as evidence while still clipping the effective value to ESO's 125% cap, so over-cap waste is visible instead of being flattened into “at cap.”

- **Extreme generated-axis adapter contract hardening** — Objective #32 generated pipeline completion, structural indexes, rotation frontier collections, runtime-policy collections, Heavy Attack channel-block proof flags, and discovered Heavy Attack denominator evidence now reject mutable/coerced or truthy non-boolean proof inputs at direct adapter boundaries.

- **Extreme finalized-event and enchantment classification hardening** — Objective #32 finalized potion resource-event evidence now rejects boolean, non-numeric, non-finite, and negative event times and requires immutable legality diagnostics; weapon-enchantment occurrence classification now requires explicit boolean damage/DoT/AoE component identity instead of truthiness coercion.

- **Extreme theoretical-closure record hardening** — Objective #32 theoretical closure now requires strict boolean finite-search and axis-coverage proof flags, validates its own immutable proof record, and the global Objective #32 result validates canonical coverage, closure, blocker, scope, and supplemental-evidence records before exposing a theoretical maximum claim.

- **Extreme proof-record consistency hardening** — Objective #32 same-tree coverage now requires immutable search/inventory proof collections and exact agreement between result and coverage-proof unresolved evidence; axis-dominance compositions validate candidate, required/dominated/missing axes, and unresolved evidence against their canonical dominance proof; closure inventory proof collections must be immutable tuples.

- **Extreme Objective #32 closure-gate hardening** — proof-promotion boundaries now reject mutable/truthy evidence across dynamic whole-plan, structural coverage, gear topology, poison consequences, enchantment runtime policy, and runtime-effect universe resolution. Final Objective #32 records require canonical search/inventory/coverage/closure/blocker inputs and reject cross-record contradictions between finite-search proof, axis debt, mechanics readiness, omitted scope, unresolved evidence, modeled DPS, and blocker closure.


- **Pre-EXE user-data persistence release gate** — Phase 14 user-owned persistence now has a consolidated fail-fast release gate covering strict Pydantic/transactional Raid Plan, Personnel, Team, Assignments, Comp identity, Build Catalog, Build Profile, Finch provenance, duplicate-player merge, and Raid Map boundaries. Read-only Saved Build and whole-user-database audits open SQLite in read-only mode, validate durable JSON directly without service constructors or migration side effects, preserve exact Saved Build counts, and combine with strict release-data classification before an EXE is approved.

- **Expanded Pydantic user-state hardening** — Phase 14 now extends fail-closed persistence contracts to Raid Section attempt/review state, Achievement progress, Settings, accessibility preferences, Collectibles, Motifs, Recipes/Furnishing Plans, and Stickerbook bookmarks. File-backed state uses validated atomic same-directory writes with fsync/read-back where applicable; SQLite writers validate intended mutations before SQL, use explicit transactions, verify affected rows before commit, and roll back on failure. Achievement mutations also restore in-memory state when persistence fails, preventing live state from drifting away from durable state. These regression modules are part of the consolidated pre-EXE release gate.

- **Extreme Objective #32 candidate closure evidence propagation**: candidate runtime scenario results now preserve strict typed sustained-DPS relevance and runtime-scaling records, and the candidate runtime-state resolver carries those records forward instead of reducing them to human-readable evidence strings. This creates the canonical seam needed for candidate-scoped closure inventory and prevents generic mechanics research debt from substituting for the mechanics actually exercised by a generated branch.
