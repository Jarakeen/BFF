# Product TODO

## Builds

- [ ] Copy Build
  - Duplicate an existing saved build as a new build for the same character.
  - Copy gear, bars, food, potion, Mundus, Champion Point selections, notes, and boss loadouts by default.
  - Require a new build name before save so the original is never overwritten.
  - Preserve character-level progression/ownership data by reference rather than duplicating it into each build.
  - Useful for per-fight variants and rapid raid setup without rebuilding the same character from scratch.
