# Old Pages

Historical UI/page implementations and archived utilities.

This directory is reference material only. Normal production runtime must not import from it. Historical tests may exercise old modules for comparison, but `engine/`, `minmax/`, `models/`, `services/`, and `ui/` may not depend on them.

See `docs/CODE_RETIREMENT_AND_QUARANTINE.md`.
